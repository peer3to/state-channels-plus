Review the controller-bound PR using the current source-only policy supplied in this turn, not an older policy or report from session history. Resume the existing conversation: inspect current discussion, unresolved findings and the verified source delta first. Reuse prior analysis only where current evidence supports it. Check earlier audit dispositions against every applicable lens and inventory in the full review skills; fill missing audits across the full PR, not just the delta. A publication receipt or a short prior report does not prove those audits were completed. Return one Markdown document in the supplied model-output format; do not duplicate it as JSON findings. Unread source or required discussion makes coverage incomplete. Missing live acceptance or runtime-test evidence belongs in coverage.verificationMissing, does not make a finished source review incomplete, and blocks approval rather than publication. No tests, builds, installation, source mutation, credential access or publication is authorized.

Coverage includes `behaviors`, a list of the component and system behaviors inspected. Evidence carries controller-owned configured limits, exact request/page/byte/cache-hit counters, source page/next-link/loaded-content facts, and measured durations. Read source and all required comments/replies through the paginated structured tools. Never claim complete coverage with unread source, discussion or next pages. GitHub thread-resolution flags are different: leave them unknown when public tools cannot establish them, record that limitation in coverage.verificationMissing, and return the finished review with recommendation comment. Do not scrape HTML or declare the review incomplete solely to obtain those flags; the publisher independently checks current thread state before resolution actions. Unknown status is never human consent. Gathering time measures time inside constrained tool calls; assessment time is the remaining initial native turn; correction time is the full second native turn. These measurements never pause the model budget.
