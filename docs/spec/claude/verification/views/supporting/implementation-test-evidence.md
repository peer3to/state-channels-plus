# Implementation Test Evidence — Supporting Migration Register

> **Agent authoring status:** Migrated from implementation documents; consolidation into matching subject verification documents is pending.

> **Engineer verification:** Pending.

This downstream register preserves concrete test evidence that cannot live in implementation documentation. Matching `verification/<subject>.md` documents are authoritative; move each entry there as its subject inventory is audited.

## architecture/contracts/architecture.md

## Test traceability

| Permutation       | Behavior                                                                              | Implementation obligations                                                                                                       | Test status | Exact test evidence                                                                                                                                                                                                                           | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `REQ-CON-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-CON-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-1.T1`. | Missing     | none — gap (measured manually from Hardhat artifacts for this document; no automated check)                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-1.T1.P2` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-CON-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-1.T1`. | Missing     | none — gap (measured manually from Hardhat artifacts for this document; no automated check)                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-CON-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-2.T1`. | Missing     | none — gap                                                                                                                                                                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-2.T1.P2` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-CON-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-2.T1`. | Missing     | none — gap                                                                                                                                                                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-CON-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-CON-3` through the matching implementation conformance row and the concrete obligations refined from `INV-CON-3.T1`. | Missing     | none — gap (convention upheld by review; exercised implicitly by all e2e suites in [test/e2e](../../../../../../test/e2e), no explicit layout check)                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-CON-3.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-CON-3` through the matching implementation conformance row and the concrete obligations refined from `INV-CON-3.T1`. | Missing     | none — gap (convention upheld by review; exercised implicitly by all e2e suites in [test/e2e](../../../../../../test/e2e), no explicit layout check)                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-CON-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-4.T1`. | Missing     | none — gap (no direct negative test found; positive path covered indirectly by [test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts) and e2e suites) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-4.T1.P2` | zero, exact balance/boundary, one beyond, maximum value and conservation              | Verify `REQ-CON-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-4.T1`. | Missing     | none — gap (no direct negative test found; positive path covered indirectly by [test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts) and e2e suites) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-4.T1.P3` | new/existing/removed/slashed participant and concurrent membership change             | Verify `REQ-CON-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-4.T1`. | Missing     | none — gap (no direct negative test found; positive path covered indirectly by [test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts) and e2e suites) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

- **Current evidence.** Deployment wiring is exercised by
  [test/V1/UniversalDeployment.test.ts](../../../../../../test/V1/UniversalDeployment.test.ts)
  (LocalDiamond and consumer-facet deployment) and by every e2e suite under
  [test/e2e](../../../../../../test/e2e), all of which deploy the full proxy+facet set through the
  harness. Facet behavior at the unit level is covered by the Foundry suites in
  [test/V1/StateChannelDiamondProxy](../../../../../../test/V1/StateChannelDiamondProxy)
  (via [DiamondHarness.sol](../../../../../../test/V1/harness/DiamondHarness.sol)).
- **Required, missing.** Size-budget enforcement (REQ-CON-2): none — gap. Storage-layout
  compatibility checks between proxy and facets: none — gap (currently guaranteed only by the
  single-inheritance convention). `onlySelf` negative tests (direct external call to each guarded
  function must revert): not found at unit level — gap; the guard is only implicitly exercised.
- **After the refactor.** Selector routing and facet replacement need dedicated tests: route
  coverage for every selector, replacement of a facet mid-lifecycle, and storage-namespace
  migration round trips.

## architecture/contracts/manager-and-facets.md

## Test traceability

| Permutation        | Behavior                                                                             | Implementation obligations                                                                                                         | Test status                        | Exact test evidence                                                                                                                                                                                                                                                                                                                                               | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ------------------ | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `REQ-CON-11.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-CON-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-11.T1`. | Partial; permutation audit pending | [test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts); [test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol](../../../../../../test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol)                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-11.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `REQ-CON-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-11.T1`. | Partial; permutation audit pending | [test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts); [test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol](../../../../../../test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol)                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-11.T1.P3` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary | Verify `REQ-CON-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-11.T1`. | Partial; permutation audit pending | [test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts); [test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol](../../../../../../test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol)                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-11.T1.P4` | zero, exact balance/boundary, one beyond, maximum value and conservation             | Verify `REQ-CON-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-11.T1`. | Partial; permutation audit pending | [test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts); [test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol](../../../../../../test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol)                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-11.T1.P5` | duplicate/replay, every relevant permutation and concurrent delivery                 | Verify `REQ-CON-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-11.T1`. | Partial; permutation audit pending | [test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts); [test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol](../../../../../../test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol)                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-11.T1.P6` | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-CON-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-11.T1`. | Partial; permutation audit pending | [test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts); [test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol](../../../../../../test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol)                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-CON-12.T1.P1` | valid case and direct invalid/opposite                                               | Verify `INV-CON-12` through the matching implementation conformance row and the concrete obligations refined from `INV-CON-12.T1`. | Partial; permutation audit pending | [E2E-StateSnapshots.test.ts](../../../../../../test/e2e/E2E-StateSnapshots.test.ts), [E2E-MaliciousUpdateSnapshot.test.ts](../../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts); direct unit test of the guard — none — gap                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-CON-12.T1.P2` | zero, exact balance/boundary, one beyond, maximum value and conservation             | Verify `INV-CON-12` through the matching implementation conformance row and the concrete obligations refined from `INV-CON-12.T1`. | Partial; permutation audit pending | [E2E-StateSnapshots.test.ts](../../../../../../test/e2e/E2E-StateSnapshots.test.ts), [E2E-MaliciousUpdateSnapshot.test.ts](../../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts); direct unit test of the guard — none — gap                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-13.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-CON-13` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-13.T1`. | Partial; permutation audit pending | Exercised via timeout/calldata e2e flows ([E2E-Timeouts.test.ts](../../../../../../test/e2e/E2E-Timeouts.test.ts), [E2E-FirstBlockTimestampGrace.test.ts](../../../../../../test/e2e/E2E-FirstBlockTimestampGrace.test.ts)); direct unit test of the three guards — none — gap                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-13.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `REQ-CON-13` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-13.T1`. | Partial; permutation audit pending | Exercised via timeout/calldata e2e flows ([E2E-Timeouts.test.ts](../../../../../../test/e2e/E2E-Timeouts.test.ts), [E2E-FirstBlockTimestampGrace.test.ts](../../../../../../test/e2e/E2E-FirstBlockTimestampGrace.test.ts)); direct unit test of the three guards — none — gap                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-13.T1.P3` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary | Verify `REQ-CON-13` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-13.T1`. | Partial; permutation audit pending | Exercised via timeout/calldata e2e flows ([E2E-Timeouts.test.ts](../../../../../../test/e2e/E2E-Timeouts.test.ts), [E2E-FirstBlockTimestampGrace.test.ts](../../../../../../test/e2e/E2E-FirstBlockTimestampGrace.test.ts)); direct unit test of the three guards — none — gap                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-13.T1.P4` | before/at/after deadline and maximum honest skew                                     | Verify `REQ-CON-13` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-13.T1`. | Partial; permutation audit pending | Exercised via timeout/calldata e2e flows ([E2E-Timeouts.test.ts](../../../../../../test/e2e/E2E-Timeouts.test.ts), [E2E-FirstBlockTimestampGrace.test.ts](../../../../../../test/e2e/E2E-FirstBlockTimestampGrace.test.ts)); direct unit test of the three guards — none — gap                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-14.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-CON-14` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-14.T1`. | Partial; permutation audit pending | Upload guards exercised in [test/e2e/disputeValidation/uploadRevert](../../../../../../test/e2e/disputeValidation/uploadRevert) and [DisputeVerificationFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`test_uploadDispute_timeoutWindowCreatedBeforeEligibility_reverts`); throttle-specific test — none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-14.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `REQ-CON-14` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-14.T1`. | Partial; permutation audit pending | Upload guards exercised in [test/e2e/disputeValidation/uploadRevert](../../../../../../test/e2e/disputeValidation/uploadRevert) and [DisputeVerificationFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`test_uploadDispute_timeoutWindowCreatedBeforeEligibility_reverts`); throttle-specific test — none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-14.T1.P3` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary | Verify `REQ-CON-14` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-14.T1`. | Partial; permutation audit pending | Upload guards exercised in [test/e2e/disputeValidation/uploadRevert](../../../../../../test/e2e/disputeValidation/uploadRevert) and [DisputeVerificationFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`test_uploadDispute_timeoutWindowCreatedBeforeEligibility_reverts`); throttle-specific test — none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-14.T1.P4` | before/at/after deadline and maximum honest skew                                     | Verify `REQ-CON-14` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-14.T1`. | Partial; permutation audit pending | Upload guards exercised in [test/e2e/disputeValidation/uploadRevert](../../../../../../test/e2e/disputeValidation/uploadRevert) and [DisputeVerificationFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`test_uploadDispute_timeoutWindowCreatedBeforeEligibility_reverts`); throttle-specific test — none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-14.T1.P5` | new/existing/removed/slashed participant and concurrent membership change            | Verify `REQ-CON-14` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-14.T1`. | Partial; permutation audit pending | Upload guards exercised in [test/e2e/disputeValidation/uploadRevert](../../../../../../test/e2e/disputeValidation/uploadRevert) and [DisputeVerificationFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`test_uploadDispute_timeoutWindowCreatedBeforeEligibility_reverts`); throttle-specific test — none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-14.T1.P6` | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-CON-14` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-14.T1`. | Partial; permutation audit pending | Upload guards exercised in [test/e2e/disputeValidation/uploadRevert](../../../../../../test/e2e/disputeValidation/uploadRevert) and [DisputeVerificationFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`test_uploadDispute_timeoutWindowCreatedBeforeEligibility_reverts`); throttle-specific test — none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-15.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-CON-15` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-15.T1`. | Partial; permutation audit pending | [DisputeVerificationFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (kill/expiry tests); [test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation) per proof type; [test/e2e/fuzz](../../../../../../test/e2e/fuzz) dispute-soundness fuzzing                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-15.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `REQ-CON-15` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-15.T1`. | Partial; permutation audit pending | [DisputeVerificationFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (kill/expiry tests); [test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation) per proof type; [test/e2e/fuzz](../../../../../../test/e2e/fuzz) dispute-soundness fuzzing                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-15.T1.P3` | before/at/after deadline and maximum honest skew                                     | Verify `REQ-CON-15` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-15.T1`. | Partial; permutation audit pending | [DisputeVerificationFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (kill/expiry tests); [test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation) per proof type; [test/e2e/fuzz](../../../../../../test/e2e/fuzz) dispute-soundness fuzzing                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-15.T1.P4` | new/existing/removed/slashed participant and concurrent membership change            | Verify `REQ-CON-15` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-15.T1`. | Partial; permutation audit pending | [DisputeVerificationFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (kill/expiry tests); [test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation) per proof type; [test/e2e/fuzz](../../../../../../test/e2e/fuzz) dispute-soundness fuzzing                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-15.T1.P5` | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-CON-15` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-15.T1`. | Partial; permutation audit pending | [DisputeVerificationFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (kill/expiry tests); [test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation) per proof type; [test/e2e/fuzz](../../../../../../test/e2e/fuzz) dispute-soundness fuzzing                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

- **Contract-level (Foundry, via [DiamondHarness.sol](../../../../../../test/V1/harness/DiamondHarness.sol)):**
  [DisputeVerificationFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol)
  (reduce precedence: slash suppresses timeout, self-removal + timeout combinations, oversized
  slash inputs, inbound-chain walk failure reporting, window observation, kill/apply expiry
  atomicity, timeout-calldata proofs),
  [FraudProofFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/FraudProofFacet.t.sol)
  (invalid-timestamp fuzzing incl. boundary/grace, wrong-turn slashing, block-author-not-participant),
  [UtilityFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/UtilityFacet.t.sol) (array-op
  fuzz properties),
  [DisputeUtils.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/utils/DisputeUtils.t.sol),
  [StateChannelManagerProxyOpen.test.sol](../../../../../../test/V1/StateChannelDiamondProxy/StateChannelManagerProxyOpen.test.sol).
- **Contract-level (Hardhat/TS):**
  [OpenChannel.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts),
  [StateProofVerification.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/StateProofVerification.test.ts),
  [SignatureVerification.test.ts](../../../../../../test/V1/DiamondProxy/UtilityLibrary/SignatureVerification.test.ts),
  [UniversalDeployment.test.ts](../../../../../../test/V1/UniversalDeployment.test.ts).
- **System-level:** the e2e suites under [test/e2e](../../../../../../test/e2e) drive every facet through
  the SDK — dispute lifecycle (`E2E-DisputeManager`, `E2E-FinalDispute`, `E2E-ReductionManager`,
  `E2E-Timeouts`, the [disputeValidation](../../../../../../test/e2e/disputeValidation) family per
  dispute-fraud-proof type, and dispute fuzzing in [fuzz](../../../../../../test/e2e/fuzz)), snapshots
  and withdrawals (`E2E-StateSnapshots`, `E2E-MaliciousUpdateSnapshot`), joins
  (`E2E-JoinChannelRaceConditions`, `E2E-ForceJoinDispute`), fraud proofs
  (`E2E-FraudProofsBlockConfirmation`), and calldata/grace timing
  (`E2E-FirstBlockTimestampGrace`).
- **Gaps worth naming:** no direct unit tests found for `postBlockCalldata`'s guard triple
  (covered only through e2e timeout flows), for the `disputerThrottle`, or for
  `multicall` revert-bubbling.

## architecture/contracts/state-machine-base.md

## Test traceability

| Permutation        | Behavior                                                                              | Implementation obligations                                                                                                         | Test status                        | Exact test evidence                                                                                                                                                                                                                                                                                                                                                                                                                   | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ------------------ | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `INV-CON-5.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-CON-5` through the matching implementation conformance row and the concrete obligations refined from `INV-CON-5.T1`.   | Partial; permutation audit pending | Implicit in all suites under [test/e2e](../../../../../../test/e2e); no dedicated round-trip property test — gap                                                                                                                                                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-CON-5.T1.P2`  | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-CON-5` through the matching implementation conformance row and the concrete obligations refined from `INV-CON-5.T1`.   | Partial; permutation audit pending | Implicit in all suites under [test/e2e](../../../../../../test/e2e); no dedicated round-trip property test — gap                                                                                                                                                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-6.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-CON-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-6.T1`.   | Partial; permutation audit pending | Off-chain/on-chain agreement exercised by [test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation); no static check or differential test — gap                                                                                                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-6.T1.P2`  | before/at/after deadline and maximum honest skew                                      | Verify `REQ-CON-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-6.T1`.   | Partial; permutation audit pending | Off-chain/on-chain agreement exercised by [test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation); no static check or differential test — gap                                                                                                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-7.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-CON-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-7.T1`.   | Partial; permutation audit pending | [test/unit/ValidationService.test.ts](../../../../../../test/unit/ValidationService.test.ts) (wrong-leader rejection); [FraudProofFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/FraudProofFacet.t.sol) `test_blockInvalidStateTransition_wrongTurnWithCorrectSnapshot_slashesSigner` covers only the in-contract-guard path; generic on-chain check: none — gap ([OQ-26](../../../specification/open-questions.md)) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-7.T1.P2`  | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-CON-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-7.T1`.   | Partial; permutation audit pending | [test/unit/ValidationService.test.ts](../../../../../../test/unit/ValidationService.test.ts) (wrong-leader rejection); [FraudProofFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/FraudProofFacet.t.sol) `test_blockInvalidStateTransition_wrongTurnWithCorrectSnapshot_slashesSigner` covers only the in-contract-guard path; generic on-chain check: none — gap ([OQ-26](../../../specification/open-questions.md)) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-8.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-CON-8` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-8.T1`.   | Partial; permutation audit pending | Balance-invariant paths in [test/e2e/disputeValidation/balanceInvariant.test.ts](../../../../../../test/e2e/disputeValidation/balanceInvariant.test.ts); no direct algebra unit test — gap                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-8.T1.P2`  | zero, exact balance/boundary, one beyond, maximum value and conservation              | Verify `REQ-CON-8` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-8.T1`.   | Partial; permutation audit pending | Balance-invariant paths in [test/e2e/disputeValidation/balanceInvariant.test.ts](../../../../../../test/e2e/disputeValidation/balanceInvariant.test.ts); no direct algebra unit test — gap                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-9.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-CON-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-9.T1`.   | Partial; permutation audit pending | Snapshot/commitment linkage exercised in [test/V1/DiamondProxy/StateChannelManager/StateProofVerification.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/StateProofVerification.test.ts) and [test/models/StateSnapshot.test.ts](../../../../../../test/models/StateSnapshot.test.ts); canonicalization property tests — none — gap                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-9.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-CON-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-9.T1`.   | Partial; permutation audit pending | Snapshot/commitment linkage exercised in [test/V1/DiamondProxy/StateChannelManager/StateProofVerification.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/StateProofVerification.test.ts) and [test/models/StateSnapshot.test.ts](../../../../../../test/models/StateSnapshot.test.ts); canonicalization property tests — none — gap                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-10.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-CON-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-10.T1`. | Partial; permutation audit pending | [E2E-ParticipantLifecycle.test.ts](../../../../../../test/e2e/E2E-ParticipantLifecycle.test.ts), [E2E-JoinChannelRaceConditions.test.ts](../../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts); direct top-up unit test — none — gap                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-10.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-CON-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-10.T1`. | Partial; permutation audit pending | [E2E-ParticipantLifecycle.test.ts](../../../../../../test/e2e/E2E-ParticipantLifecycle.test.ts), [E2E-JoinChannelRaceConditions.test.ts](../../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts); direct top-up unit test — none — gap                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-10.T1.P3` | zero, exact balance/boundary, one beyond, maximum value and conservation              | Verify `REQ-CON-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-10.T1`. | Partial; permutation audit pending | [E2E-ParticipantLifecycle.test.ts](../../../../../../test/e2e/E2E-ParticipantLifecycle.test.ts), [E2E-JoinChannelRaceConditions.test.ts](../../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts); direct top-up unit test — none — gap                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CON-10.T1.P4` | new/existing/removed/slashed participant and concurrent membership change             | Verify `REQ-CON-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-CON-10.T1`. | Partial; permutation audit pending | [E2E-ParticipantLifecycle.test.ts](../../../../../../test/e2e/E2E-ParticipantLifecycle.test.ts), [E2E-JoinChannelRaceConditions.test.ts](../../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts); direct top-up unit test — none — gap                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

- **Round trip (INV-CON-5) and hook behavior:** exercised continuously by every e2e suite (all SDK
  execution replays through `getState`/`_setState`), but there is no dedicated
  property/round-trip unit test for the reference machines — gap.
- **Wrong-turn rejection (REQ-CON-7):** on-chain side covered by
  `test_blockInvalidStateTransition_wrongTurnWithCorrectSnapshot_slashesSigner` in
  [FraudProofFacet.t.sol](../../../../../../test/V1/StateChannelDiamondProxy/FraudProofFacet.t.sol);
  end-to-end fraud path in
  [E2E-FraudProofsBlockConfirmation.test.ts](../../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts).
- **Transition execution & lifecycle:**
  [E2E-StateTransition.test.ts](../../../../../../test/e2e/E2E-StateTransition.test.ts),
  [E2E-ParticipantLifecycle.test.ts](../../../../../../test/e2e/E2E-ParticipantLifecycle.test.ts)
  (join/remove/slash/exit through the real pipeline).
- **Determinism across environments (REQ-CON-6):** verified implicitly wherever off-chain execution
  and on-chain replay must agree (dispute-validation suites under
  [test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation)); no targeted
  same-input/same-output differential test — gap.
- **What a new integration must test locally:** state round trips (including every reachable shape
  of the state struct), the balance algebra (underflow rejection, commutativity/associativity of
  `addBalance` as used, zero element), wrong-author rejection for every public transition,
  `_joinChannel` in both admission and top-up cases, and genesis balance-invariant satisfaction.

## architecture/sdk/architecture.md

## Test traceability

| Permutation       | Behavior                                                                              | Implementation obligations                                                                                                       | Test status                        | Exact test evidence                                                                                                                                                                   | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `REQ-SDK-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-SDK-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-SDK-1.T1`. | Partial; permutation audit pending | e2e suites construct peers exclusively via `signerSecret` (e.g. [test/harness](../../../../../../test/harness)) — implicit; no direct negative test (`none — gap` for rejection path) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SDK-1.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-SDK-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-SDK-1.T1`. | Partial; permutation audit pending | e2e suites construct peers exclusively via `signerSecret` (e.g. [test/harness](../../../../../../test/harness)) — implicit; no direct negative test (`none — gap` for rejection path) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SDK-1.T1.P3` | before/at/after deadline and maximum honest skew                                      | Verify `REQ-SDK-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-SDK-1.T1`. | Partial; permutation audit pending | e2e suites construct peers exclusively via `signerSecret` (e.g. [test/harness](../../../../../../test/harness)) — implicit; no direct negative test (`none — gap` for rejection path) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SDK-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-SDK-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-SDK-2.T1`. | Missing                            | none — gap (no partitioned/dishonest-RPC test)                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SDK-2.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-SDK-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-SDK-2.T1`. | Missing                            | none — gap (no partitioned/dishonest-RPC test)                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SDK-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-SDK-1` through the matching implementation conformance row and the concrete obligations refined from `INV-SDK-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts)                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SDK-1.T1.P2` | before/at/after deadline and maximum honest skew                                      | Verify `INV-SDK-1` through the matching implementation conformance row and the concrete obligations refined from `INV-SDK-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts)                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SDK-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-SDK-2` through the matching implementation conformance row and the concrete obligations refined from `INV-SDK-2.T1`. | Partial; permutation audit pending | concurrency e2e suites (dispute/reduction races) exercise it indirectly; none — gap (no dedicated nonce-race test)                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SDK-2.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-SDK-2` through the matching implementation conformance row and the concrete obligations refined from `INV-SDK-2.T1`. | Partial; permutation audit pending | concurrency e2e suites (dispute/reduction races) exercise it indirectly; none — gap (no dedicated nonce-race test)                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SDK-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-SDK-3` through the matching implementation conformance row and the concrete obligations refined from `INV-SDK-3.T1`. | Partial; permutation audit pending | [test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation) suites replay disputes while live execution continues                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SDK-3.T1.P2` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `INV-SDK-3` through the matching implementation conformance row and the concrete obligations refined from `INV-SDK-3.T1`. | Partial; permutation audit pending | [test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation) suites replay disputes while live execution continues                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

- Runtime modes and port protocol: [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts),
  [test/e2e/E2E-WorkerShutdown.test.ts](../../../../../../test/e2e/E2E-WorkerShutdown.test.ts).
- Event surface across the port: [test/e2e/E2E-RuntimeContractEvents.test.ts](../../../../../../test/e2e/E2E-RuntimeContractEvents.test.ts),
  [test/unit/EventBus.test.ts](../../../../../../test/unit/EventBus.test.ts),
  [test/stateManager/EventBus.test.ts](../../../../../../test/stateManager/EventBus.test.ts).
- Abort semantics: [test/stateManager/StateManagerAbort.test.ts](../../../../../../test/stateManager/StateManagerAbort.test.ts).
- Gap: no test exercises behavior under a failing/partitioned RPC provider
  (the REQ-SDK-2 assumption is asserted, not tested).

## architecture/sdk/block-confirmation-pipeline.md

## Test traceability

| Permutation       | Behavior                                                                              | Implementation obligations                                                                                                       | Test status                        | Exact test evidence                                                                                                                                                                                                  | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `INV-BCP-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-BCP-1` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-StateTransition.test.ts](../../../../../../test/e2e/E2E-StateTransition.test.ts)                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-1.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-BCP-1` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-StateTransition.test.ts](../../../../../../test/e2e/E2E-StateTransition.test.ts)                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-BCP-2` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FraudProofsBlockConfirmation.test.ts](../../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts) (continued progress after rejected blocks)                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-2.T1.P2` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `INV-BCP-2` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FraudProofsBlockConfirmation.test.ts](../../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts) (continued progress after rejected blocks)                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-BCP-3` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-BlockQueueManager.test.ts](../../../../../../test/e2e/E2E-BlockQueueManager.test.ts)                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-3.T1.P2` | duplicate/replay, every relevant permutation and concurrent delivery                  | Verify `INV-BCP-3` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-BlockQueueManager.test.ts](../../../../../../test/e2e/E2E-BlockQueueManager.test.ts)                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-BCP-4` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-4.T1`. | Partial; permutation audit pending | [test/storage/QueueStorage.test.ts](../../../../../../test/storage/QueueStorage.test.ts), [test/e2e/E2E-ByzantineErrorAttribution.test.ts](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts)         | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-4.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-BCP-4` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-4.T1`. | Partial; permutation audit pending | [test/storage/QueueStorage.test.ts](../../../../../../test/storage/QueueStorage.test.ts), [test/e2e/E2E-ByzantineErrorAttribution.test.ts](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts)         | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-4.T1.P3` | before/at/after deadline and maximum honest skew                                      | Verify `INV-BCP-4` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-4.T1`. | Partial; permutation audit pending | [test/storage/QueueStorage.test.ts](../../../../../../test/storage/QueueStorage.test.ts), [test/e2e/E2E-ByzantineErrorAttribution.test.ts](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts)         | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-4.T1.P4` | duplicate/replay, every relevant permutation and concurrent delivery                  | Verify `INV-BCP-4` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-4.T1`. | Partial; permutation audit pending | [test/storage/QueueStorage.test.ts](../../../../../../test/storage/QueueStorage.test.ts), [test/e2e/E2E-ByzantineErrorAttribution.test.ts](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts)         | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-5.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-BCP-5` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-5.T1`. | Partial; permutation audit pending | [test/e2e/E2E-StateTransition.test.ts](../../../../../../test/e2e/E2E-StateTransition.test.ts) (echo dedup)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-5.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-BCP-5` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-5.T1`. | Partial; permutation audit pending | [test/e2e/E2E-StateTransition.test.ts](../../../../../../test/e2e/E2E-StateTransition.test.ts) (echo dedup)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-6.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-BCP-6` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-6.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FraudProofsBlockConfirmation.test.ts](../../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts)                                                                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-6.T1.P2` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `INV-BCP-6` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-6.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FraudProofsBlockConfirmation.test.ts](../../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts)                                                                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-7.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-BCP-7` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-7.T1`. | Partial; permutation audit pending | [test/e2e/E2E-Timeouts.test.ts](../../../../../../test/e2e/E2E-Timeouts.test.ts)                                                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-BCP-7.T1.P2` | new/existing/removed/slashed participant and concurrent membership change             | Verify `INV-BCP-7` through the matching implementation conformance row and the concrete obligations refined from `INV-BCP-7.T1`. | Partial; permutation audit pending | [test/e2e/E2E-Timeouts.test.ts](../../../../../../test/e2e/E2E-Timeouts.test.ts)                                                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-BCP-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-StateTransition.test.ts](../../../../../../test/e2e/E2E-StateTransition.test.ts), [test/e2e/E2E-Timeouts.test.ts](../../../../../../test/e2e/E2E-Timeouts.test.ts) (calldata recovery)                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-1.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-BCP-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-StateTransition.test.ts](../../../../../../test/e2e/E2E-StateTransition.test.ts), [test/e2e/E2E-Timeouts.test.ts](../../../../../../test/e2e/E2E-Timeouts.test.ts) (calldata recovery)                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-1.T1.P3` | before/at/after deadline and maximum honest skew                                      | Verify `REQ-BCP-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-StateTransition.test.ts](../../../../../../test/e2e/E2E-StateTransition.test.ts), [test/e2e/E2E-Timeouts.test.ts](../../../../../../test/e2e/E2E-Timeouts.test.ts) (calldata recovery)                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-BCP-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FirstBlockTimestampGrace.test.ts](../../../../../../test/e2e/E2E-FirstBlockTimestampGrace.test.ts)                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-2.T1.P2` | before/at/after deadline and maximum honest skew                                      | Verify `REQ-BCP-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FirstBlockTimestampGrace.test.ts](../../../../../../test/e2e/E2E-FirstBlockTimestampGrace.test.ts)                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-2.T1.P3` | static review of every named alternative, omitted category and changed assumption     | Verify `REQ-BCP-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FirstBlockTimestampGrace.test.ts](../../../../../../test/e2e/E2E-FirstBlockTimestampGrace.test.ts)                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-BCP-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-3.T1`. | Partial; permutation audit pending | concurrency exercised by [test/e2e/E2E-StateTransition.test.ts](../../../../../../test/e2e/E2E-StateTransition.test.ts) and parallel-runner suites; dedicated mutex-freedom/merge-idempotence unit tests: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-3.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-BCP-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-3.T1`. | Partial; permutation audit pending | concurrency exercised by [test/e2e/E2E-StateTransition.test.ts](../../../../../../test/e2e/E2E-StateTransition.test.ts) and parallel-runner suites; dedicated mutex-freedom/merge-idempotence unit tests: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-3.T1.P3` | duplicate/replay, every relevant permutation and concurrent delivery                  | Verify `REQ-BCP-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-3.T1`. | Partial; permutation audit pending | concurrency exercised by [test/e2e/E2E-StateTransition.test.ts](../../../../../../test/e2e/E2E-StateTransition.test.ts) and parallel-runner suites; dedicated mutex-freedom/merge-idempotence unit tests: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-BCP-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-4.T1`. | Partial; permutation audit pending | [test/unit/ValidationService.test.ts](../../../../../../test/unit/ValidationService.test.ts) (conflict rejection); same-coordinate competing-body race test: none — gap                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-4.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-BCP-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-4.T1`. | Partial; permutation audit pending | [test/unit/ValidationService.test.ts](../../../../../../test/unit/ValidationService.test.ts) (conflict rejection); same-coordinate competing-body race test: none — gap                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-4.T1.P3` | duplicate/replay, every relevant permutation and concurrent delivery                  | Verify `REQ-BCP-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-4.T1`. | Partial; permutation audit pending | [test/unit/ValidationService.test.ts](../../../../../../test/unit/ValidationService.test.ts) (conflict rejection); same-coordinate competing-body race test: none — gap                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-BCP-4.T1.P4` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-BCP-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-BCP-4.T1`. | Partial; permutation audit pending | [test/unit/ValidationService.test.ts](../../../../../../test/unit/ValidationService.test.ts) (conflict rejection); same-coordinate competing-body race test: none — gap                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

Unit/component: [test/stateManager/ValidationService.test.ts](../../../../../../test/stateManager/ValidationService.test.ts),
[test/unit/ValidationService.test.ts](../../../../../../test/unit/ValidationService.test.ts),
[test/storage/QueueStorage.test.ts](../../../../../../test/storage/QueueStorage.test.ts),
[test/storage/BlockStorage.test.ts](../../../../../../test/storage/BlockStorage.test.ts),
[test/unit/AgreementManager.test.ts](../../../../../../test/unit/AgreementManager.test.ts),
[test/models/Block.test.ts](../../../../../../test/models/Block.test.ts),
[test/stateManager/StateManagerTimeout.test.ts](../../../../../../test/stateManager/StateManagerTimeout.test.ts).
End-to-end: [E2E-StateTransition](../../../../../../test/e2e/E2E-StateTransition.test.ts),
[E2E-BlockQueueManager](../../../../../../test/e2e/E2E-BlockQueueManager.test.ts)
(queue lifetime, fork gates, sync probes),
[E2E-FraudProofsBlockConfirmation](../../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts)
(escalation per fraud class),
[E2E-FirstBlockTimestampGrace](../../../../../../test/e2e/E2E-FirstBlockTimestampGrace.test.ts),
[E2E-ByzantineErrorAttribution](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts)
(stray-signature attribution), [E2E-Spectate](../../../../../../test/e2e/E2E-Spectate.test.ts)
and [E2E-SpectatingAbortDoS](../../../../../../test/e2e/E2E-SpectatingAbortDoS.test.ts)
(spectator fail-closed vs DoS), [E2E-Timeouts](../../../../../../test/e2e/E2E-Timeouts.test.ts).
Gaps: no partitioned-network or delayed-signature adversarial suite explicitly
targeting virtual-vote finality claims; no gossip-flood/rate-limit test
([OQ-6](../../../specification/open-questions.md)).

## architecture/sdk/components.md

## Test traceability

| Permutation       | Behavior                                                                              | Implementation obligations                                                                                                       | Test status                        | Exact test evidence                                                                                                                                                              | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `REQ-SDK-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-SDK-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-SDK-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts), [test/rpc/initHandshake](../../../../../../test/rpc/initHandshake)                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SDK-3.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-SDK-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-SDK-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts), [test/rpc/initHandshake](../../../../../../test/rpc/initHandshake)                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SDK-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-SDK-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-SDK-4.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts), [test/e2e/E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SDK-4.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-SDK-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-SDK-4.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts), [test/e2e/E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SDK-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-SDK-4` through the matching implementation conformance row and the concrete obligations refined from `INV-SDK-4.T1`. | Partial; permutation audit pending | [test/storage/BlockStorage.test.ts](../../../../../../test/storage/BlockStorage.test.ts)                                                                                         | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SDK-4.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-SDK-4` through the matching implementation conformance row and the concrete obligations refined from `INV-SDK-4.T1`. | Partial; permutation audit pending | [test/storage/BlockStorage.test.ts](../../../../../../test/storage/BlockStorage.test.ts)                                                                                         | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SDK-5.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-SDK-5` through the matching implementation conformance row and the concrete obligations refined from `INV-SDK-5.T1`. | Partial; permutation audit pending | [test/stateManager/EventSyncService.test.ts](../../../../../../test/stateManager/EventSyncService.test.ts)                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SDK-5.T1.P2` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `INV-SDK-5` through the matching implementation conformance row and the concrete obligations refined from `INV-SDK-5.T1`. | Partial; permutation audit pending | [test/stateManager/EventSyncService.test.ts](../../../../../../test/stateManager/EventSyncService.test.ts)                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SDK-6.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-SDK-6` through the matching implementation conformance row and the concrete obligations refined from `INV-SDK-6.T1`. | Partial; permutation audit pending | [test/e2e/E2E-ByzantineErrorAttribution.test.ts](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts); none — gap (no dedicated upgrade-blacklist test)             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SDK-6.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-SDK-6` through the matching implementation conformance row and the concrete obligations refined from `INV-SDK-6.T1`. | Partial; permutation audit pending | [test/e2e/E2E-ByzantineErrorAttribution.test.ts](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts); none — gap (no dedicated upgrade-blacklist test)             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

- **Purpose.** Interpretation layer over `Storage`: turns stored blocks,
  snapshots, and participant-change points into agreement facts and state
  proofs. Exists so that signature/threshold semantics live in one place
  instead of being re-derived by every pipeline.
- **Contract.** `didEveryoneSignBlock(block)` — threshold over the block's
  participant union. `getLatestSignedBlockByParticipant(fork, addr)` — newest
  block carrying the participant's signature (descending walk).
  `getStateProof(fork, height)` / `tryGetStateProof` — milestones per
  participant-set change point plus a latest milestone, else a linked
  `signedBlocks` suffix; never consumes a block above the requested height
  ([../protocol/state-proofs.md](../../../specification/disputes/state-proofs.md)).
  `getReduceData(fork, reduceOutput)` — resolves a reduction output to its
  snapshot, machine state, and consumed inbound range.
- **Dependencies.** `Storage` only (pure reads); no chain access.
- **Guarantees.** Milestone construction implements virtual voting: threshold
  signers = previous anchor's participants ∪ the lowest block's resulting
  participants; signatures collected across later linked blocks count for the
  anchor. Built milestones are minimal (signatures filtered to the threshold
  set).
- **Non-guarantees.** No validation of the underlying blocks (the pipeline
  validated them before storage); throws on locally missing snapshots/states.
- **Verification.** [test/unit/AgreementManager.test.ts](../../../../../../test/unit/AgreementManager.test.ts);
  proof shape exercised by the dispute e2e suites.

- **Non-guarantees.** No delivery ordering across peers, no retry, no gossip
  rate limiting ([OQ-6](../../../specification/open-questions.md) — policy undesigned).
- **Verification.** [test/rpc](../../../../../../test/rpc),
  [E2E-InitHandshake](../../../../../../test/e2e/E2E-InitHandshake.test.ts),
  [E2E-CustomRpcRequestResponse](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts),
  [E2E-IsForkDisputed](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts),
  [test/unit/SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts),
  [E2E-Spectate](../../../../../../test/e2e/E2E-Spectate.test.ts).

- **Non-guarantees.** No transport-level authentication (identity is
  established by the handshake service), no delivery guarantees.
- **Verification.** [E2E-RuntimeTransportModes](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts);
  WebRTC upgrade paths in the browser suites ([test/browser](../../../../../../test/browser)).

- **Non-guarantees.** No persistence across restart; no pruning (unbounded
  growth over channel lifetime). **Open question:** restart/recovery semantics
  are unspecified — currently a restarted peer must resync as a spectator.
- **Verification.** [test/storage](../../../../../../test/storage) unit suites.

[`src/Clock.ts`](../../../../../../src/Clock.ts) — process-wide singleton aligned to
chain time (protocol model: [../protocol/time.md](../../../specification/protocol-model/time.md)).
`init(provider)` measures the offset between local wall time and the latest
block timestamp, iterating until the offset is within one average block time
(average over ≤10 recent blocks); re-initializes when a new provider arrives.
`getTimeInSeconds()` = local time + adjustment (synchronous, used by every
deadline in both pipelines); `getBlockchainTime()` reads the latest block;
`getAverageOnChainBlockTime()` feeds event-recovery span estimates.
Non-guarantees: no continuous re-sync after init; drift between syncs is
bounded only by the protocol's time tolerances. Verification:
[test/Clock.test.ts](../../../../../../test/Clock.test.ts).

- [`StateChannelEventListener`](../../../../../../src/StateChannelEventListener.ts)
  subscribes one provider filter per channel (11 manager event topics filtered
  by channelId) and forwards every log to `EventSyncService`; generation
  counter + dispose barrier prevent stale delivery.
- [`EventSyncService`](../../../../../../src/stateManager/EventSyncService.ts)
  deduplicates logs by `(address, txHash, logIndex)`, tracks per-chain-block
  completion to publish a processed-block watermark (never past a failed log),
  dispatches to `EventHandler`, and owns targeted recovery: calldata
  commitments (`tryRecoverBlockCalldataAndScheduleValidation`) and missing
  dispute events (`ensureDisputesProcessed`) are re-queried from the chain so
  consumers never act on a window their storage cannot back. A failed log
  handler is fatal for that log: it stays cached rejected and its block never
  completes.
- [`EventHandler`](../../../../../../src/eventHandlers/EventHandler.ts) is the
  semantic layer: it mirrors every event **in its supported set** into the
  `LocalDiamond` first, then applies SDK effects (genesis install on
  `ChannelOpened`, status transitions and channel close on
  `StateSnapshotUpdated`, block ingest on `BlockCalldataPosted`, the whole
  dispute intake of [dispute-pipeline.md](../../../implementation/views/architecture/sdk/dispute-pipeline.md) §3.3/§6,
  inbound-message capture on `InboundMessagesProcessed`). The supported set is
  not the full contract event set: `OutboundMessagesProcessed` is not
  dispatched ([DEF-2](../../../specification/open-questions.md#implementation-defects-with-a-proposed-direction))
  — `WithdrawalsUpdated` mirrors the aggregate withdrawal balance, but whether
  it is the intended complete substitute (outbound height/history) is an open
  decision. Ordering: the local-EVM mirror is
  gated by `(blockNumber, logIndex)` coordinates; a code TODO notes the TS
  side of `onStateSnapshotUpdated` is not yet coordinate-gated —
  **Open question:** whether out-of-order snapshot events can race the TS
  handler.
- **Verification.** [test/stateManager/EventSyncService.test.ts](../../../../../../test/stateManager/EventSyncService.test.ts),
  [test/storage/EventSyncStorage.test.ts](../../../../../../test/storage/EventSyncStorage.test.ts),
  dispute/e2e suites exercise the handlers.

[`src/events/EventBus.ts`](../../../../../../src/events/EventBus.ts) — the one event
surface in every realm; kinds `p2pEventHooks`, `eventHandler`,
`contractEvents`. Contract: named listeners, kind-wide listeners, and
runtime-owned kind-wide adapters (survive `clear()`); dispatch order
exact-name → kind-wide → the single host-only bridge tap whose failure
propagates to the producer while local listeners stay isolated.
`attachContractEvents` mirrors bus contract events onto any structural ethers
instance; `createBusPublishingHooks` wraps app hooks so every hook call
publishes on the bus first. Details in [architecture.md](../../../implementation/views/architecture/sdk/architecture.md) §5.
Verification: [test/unit/EventBus.test.ts](../../../../../../test/unit/EventBus.test.ts),
[test/stateManager/EventBus.test.ts](../../../../../../test/stateManager/EventBus.test.ts).

- [`Block`](../../../../../../src/models/Block.ts): lazy encode/hash, author +
  signer recovery (globally memoized), monotone `expandSignatures` /
  `removeConfirmationSignatures` (author signature is never removable),
  `didEveryoneSign`, `getRelevantTimestamp(nextAuthor)` — block timestamp if
  the next author signed it, else `max(onChainTimestamp, timestamp)`; the
  timestamp base for turn deadlines and forfeiture rules.
- [`StateSnapshot`](../../../../../../src/models/StateSnapshot.ts): snapshot +
  `snapshotData` hashing (`forkId` of a genesis = hash of its `snapshotData`).
- [`StateProof`](../../../../../../src/models/StateProof.ts): safe decode
  (`tryFrom`) of proof structs — undecodable peer data becomes `null`, not a
  crash.
- **Verification.** [test/models](../../../../../../test/models).

## architecture/sdk/dispute-pipeline.md

## Test traceability

| Permutation       | Behavior                                                                    | Implementation obligations                                                                                                       | Test status                        | Exact test evidence                                                                                                                                                                                                                                                                  | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `INV-DVP-1.T1.P1` | valid case and direct invalid/opposite                                      | Verify `INV-DVP-1` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-DisputeManager.test.ts](../../../../../../test/e2e/E2E-DisputeManager.test.ts)                                                                                                                                                                                         | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-1.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork | Verify `INV-DVP-1` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-DisputeManager.test.ts](../../../../../../test/e2e/E2E-DisputeManager.test.ts)                                                                                                                                                                                         | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-1.T1.P3` | duplicate/replay, every relevant permutation and concurrent delivery        | Verify `INV-DVP-1` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-DisputeManager.test.ts](../../../../../../test/e2e/E2E-DisputeManager.test.ts)                                                                                                                                                                                         | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-1.T1.P4` | malformed and adversarial input, partial failure, retry and recovery        | Verify `INV-DVP-1` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-DisputeManager.test.ts](../../../../../../test/e2e/E2E-DisputeManager.test.ts)                                                                                                                                                                                         | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-2.T1.P1` | valid case and direct invalid/opposite                                      | Verify `INV-DVP-2` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-2.T1`. | Partial; permutation audit pending | [test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation) suites                                                                                                                                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-2.T1.P2` | new/existing/removed/slashed participant and concurrent membership change   | Verify `INV-DVP-2` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-2.T1`. | Partial; permutation audit pending | [test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation) suites                                                                                                                                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-3.T1.P1` | valid case and direct invalid/opposite                                      | Verify `INV-DVP-3` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-3.T1`. | Partial; permutation audit pending | [test/stateManager/DisputeValidationStrategy.test.ts](../../../../../../test/stateManager/DisputeValidationStrategy.test.ts)                                                                                                                                                         | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-3.T1.P2` | malformed and adversarial input, partial failure, retry and recovery        | Verify `INV-DVP-3` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-3.T1`. | Partial; permutation audit pending | [test/stateManager/DisputeValidationStrategy.test.ts](../../../../../../test/stateManager/DisputeValidationStrategy.test.ts)                                                                                                                                                         | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-4.T1.P1` | valid case and direct invalid/opposite                                      | Verify `INV-DVP-4` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-4.T1`. | Partial; permutation audit pending | [test/e2e/E2E-Fuzz-Dispute-MVP.test.ts](../../../../../../test/e2e/E2E-Fuzz-Dispute-MVP.test.ts), [test/unit/ReductionExecutor.test.ts](../../../../../../test/unit/ReductionExecutor.test.ts)                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-4.T1.P2` | duplicate/replay, every relevant permutation and concurrent delivery        | Verify `INV-DVP-4` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-4.T1`. | Partial; permutation audit pending | [test/e2e/E2E-Fuzz-Dispute-MVP.test.ts](../../../../../../test/e2e/E2E-Fuzz-Dispute-MVP.test.ts), [test/unit/ReductionExecutor.test.ts](../../../../../../test/unit/ReductionExecutor.test.ts)                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-5.T1.P1` | valid case and direct invalid/opposite                                      | Verify `INV-DVP-5` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-5.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FinalDispute.test.ts](../../../../../../test/e2e/E2E-FinalDispute.test.ts), [test/e2e/E2E-ReductionManager.test.ts](../../../../../../test/e2e/E2E-ReductionManager.test.ts)                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-5.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork | Verify `INV-DVP-5` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-5.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FinalDispute.test.ts](../../../../../../test/e2e/E2E-FinalDispute.test.ts), [test/e2e/E2E-ReductionManager.test.ts](../../../../../../test/e2e/E2E-ReductionManager.test.ts)                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-5.T1.P3` | malformed and adversarial input, partial failure, retry and recovery        | Verify `INV-DVP-5` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-5.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FinalDispute.test.ts](../../../../../../test/e2e/E2E-FinalDispute.test.ts), [test/e2e/E2E-ReductionManager.test.ts](../../../../../../test/e2e/E2E-ReductionManager.test.ts)                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-6.T1.P1` | valid case and direct invalid/opposite                                      | Verify `INV-DVP-6` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-6.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FraudProofsBlockConfirmation.test.ts](../../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts)                                                                                                                                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-6.T1.P2` | new/existing/removed/slashed participant and concurrent membership change   | Verify `INV-DVP-6` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-6.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FraudProofsBlockConfirmation.test.ts](../../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts)                                                                                                                                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DVP-6.T1.P3` | malformed and adversarial input, partial failure, retry and recovery        | Verify `INV-DVP-6` through the matching implementation conformance row and the concrete obligations refined from `INV-DVP-6.T1`. | Partial; permutation audit pending | [test/e2e/E2E-FraudProofsBlockConfirmation.test.ts](../../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts)                                                                                                                                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DVP-1.T1.P1` | valid case and direct invalid/opposite                                      | Verify `REQ-DVP-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-DVP-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-Timeouts.test.ts](../../../../../../test/e2e/E2E-Timeouts.test.ts)                                                                                                                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DVP-1.T1.P2` | before/at/after deadline and maximum honest skew                            | Verify `REQ-DVP-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-DVP-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-Timeouts.test.ts](../../../../../../test/e2e/E2E-Timeouts.test.ts)                                                                                                                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DVP-2.T1.P1` | valid case and direct invalid/opposite                                      | Verify `REQ-DVP-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-DVP-2.T1`. | Partial; permutation audit pending | [test/stateManager/EventSyncService.test.ts](../../../../../../test/stateManager/EventSyncService.test.ts)                                                                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DVP-2.T1.P2` | before/at/after deadline and maximum honest skew                            | Verify `REQ-DVP-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-DVP-2.T1`. | Partial; permutation audit pending | [test/stateManager/EventSyncService.test.ts](../../../../../../test/stateManager/EventSyncService.test.ts)                                                                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DVP-2.T1.P3` | malformed and adversarial input, partial failure, retry and recovery        | Verify `REQ-DVP-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-DVP-2.T1`. | Partial; permutation audit pending | [test/stateManager/EventSyncService.test.ts](../../../../../../test/stateManager/EventSyncService.test.ts)                                                                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DVP-3.T1.P1` | valid case and direct invalid/opposite                                      | Verify `REQ-DVP-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-DVP-3.T1`. | Partial; permutation audit pending | [test/e2e/disputeValidation/reducedForkTimestampMismatch.test.ts](../../../../../../test/e2e/disputeValidation/reducedForkTimestampMismatch.test.ts), [test/stateManager/DisputeReductionStaleEvent.test.ts](../../../../../../test/stateManager/DisputeReductionStaleEvent.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DVP-3.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork | Verify `REQ-DVP-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-DVP-3.T1`. | Partial; permutation audit pending | [test/e2e/disputeValidation/reducedForkTimestampMismatch.test.ts](../../../../../../test/e2e/disputeValidation/reducedForkTimestampMismatch.test.ts), [test/stateManager/DisputeReductionStaleEvent.test.ts](../../../../../../test/stateManager/DisputeReductionStaleEvent.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DVP-3.T1.P3` | before/at/after deadline and maximum honest skew                            | Verify `REQ-DVP-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-DVP-3.T1`. | Partial; permutation audit pending | [test/e2e/disputeValidation/reducedForkTimestampMismatch.test.ts](../../../../../../test/e2e/disputeValidation/reducedForkTimestampMismatch.test.ts), [test/stateManager/DisputeReductionStaleEvent.test.ts](../../../../../../test/stateManager/DisputeReductionStaleEvent.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

Unit/component: [test/unit/DisputeManager.test.ts](../../../../../../test/unit/DisputeManager.test.ts),
[test/unit/ReductionExecutor.test.ts](../../../../../../test/unit/ReductionExecutor.test.ts),
[test/stateManager/ReductionManager.test.ts](../../../../../../test/stateManager/ReductionManager.test.ts),
[test/stateManager/DisputeValidationStrategy.test.ts](../../../../../../test/stateManager/DisputeValidationStrategy.test.ts),
[test/stateManager/DisputeReductionStaleEvent.test.ts](../../../../../../test/stateManager/DisputeReductionStaleEvent.test.ts),
[test/stateManager/SnapshotUpdateService.test.ts](../../../../../../test/stateManager/SnapshotUpdateService.test.ts),
[test/storage/DisputeStorage.test.ts](../../../../../../test/storage/DisputeStorage.test.ts).
End-to-end: [E2E-DisputeManager](../../../../../../test/e2e/E2E-DisputeManager.test.ts),
[E2E-FinalDispute](../../../../../../test/e2e/E2E-FinalDispute.test.ts),
[E2E-ReductionManager](../../../../../../test/e2e/E2E-ReductionManager.test.ts),
[E2E-Timeouts](../../../../../../test/e2e/E2E-Timeouts.test.ts),
[E2E-ForceJoinDispute](../../../../../../test/e2e/E2E-ForceJoinDispute.test.ts),
[E2E-StaleMembershipDispute](../../../../../../test/e2e/E2E-StaleMembershipDispute.test.ts),
the per-proof-type kill suites under
[test/e2e/disputeValidation](../../../../../../test/e2e/disputeValidation)
(balance invariant, inbound hash, not-latest-state, output state, state-proof
structure/genesis linkage, future block, upload reverts), and permutation
fuzzing in [E2E-Fuzz-Dispute-MVP](../../../../../../test/e2e/E2E-Fuzz-Dispute-MVP.test.ts)
plus [test/e2e/fuzz](../../../../../../test/e2e/fuzz) (order-independence evidence
for INV-DVP-4).
Gaps: no test for the audit-skip paths (undecodable proof without posted data;
anchor unavailable locally); no adversarial RPC test for kill-deadline misses.

## architecture/sdk/rpc/handshake.md

## Test traceability

| Permutation       | Behavior                                                                              | Implementation obligations                                                                                                       | Test status                        | Exact test evidence                                                                                                                                                             | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `INV-HSK-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-HSK-1` through the matching implementation conformance row and the concrete obligations refined from `INV-HSK-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts) (completion + profile); none — gap (no single-direction-does-not-complete assertion) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-HSK-1.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-HSK-1` through the matching implementation conformance row and the concrete obligations refined from `INV-HSK-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts) (completion + profile); none — gap (no single-direction-does-not-complete assertion) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-HSK-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-HSK-2` through the matching implementation conformance row and the concrete obligations refined from `INV-HSK-2.T1`. | Partial; permutation audit pending | [test/rpc/initHandshake/InitHandshakeChallenge.test.ts](../../../../../../test/rpc/initHandshake/InitHandshakeChallenge.test.ts)                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-HSK-2.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-HSK-2` through the matching implementation conformance row and the concrete obligations refined from `INV-HSK-2.T1`. | Partial; permutation audit pending | [test/rpc/initHandshake/InitHandshakeChallenge.test.ts](../../../../../../test/rpc/initHandshake/InitHandshakeChallenge.test.ts)                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-HSK-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-HSK-3` through the matching implementation conformance row and the concrete obligations refined from `INV-HSK-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts) (time-skew cases); none — gap (no explicit non-hex32 / NaN unit test)                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-HSK-3.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-HSK-3` through the matching implementation conformance row and the concrete obligations refined from `INV-HSK-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts) (time-skew cases); none — gap (no explicit non-hex32 / NaN unit test)                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-HSK-3.T1.P3` | before/at/after deadline and maximum honest skew                                      | Verify `INV-HSK-3` through the matching implementation conformance row and the concrete obligations refined from `INV-HSK-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts) (time-skew cases); none — gap (no explicit non-hex32 / NaN unit test)                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-HSK-3.T1.P4` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `INV-HSK-3` through the matching implementation conformance row and the concrete obligations refined from `INV-HSK-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts) (time-skew cases); none — gap (no explicit non-hex32 / NaN unit test)                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-HSK-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-HSK-4` through the matching implementation conformance row and the concrete obligations refined from `INV-HSK-4.T1`. | Missing                            | none — gap (no test feeds a mismatched ack challenge)                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-HSK-4.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `INV-HSK-4` through the matching implementation conformance row and the concrete obligations refined from `INV-HSK-4.T1`. | Missing                            | none — gap (no test feeds a mismatched ack challenge)                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-HSK-5.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-HSK-5` through the matching implementation conformance row and the concrete obligations refined from `INV-HSK-5.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts) (Time Validation)                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-HSK-5.T1.P2` | before/at/after deadline and maximum honest skew                                      | Verify `INV-HSK-5` through the matching implementation conformance row and the concrete obligations refined from `INV-HSK-5.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts) (Time Validation)                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-HSK-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-HSK-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-HSK-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts); none — gap (no MITM/relay or flood suite)                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-HSK-1.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-HSK-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-HSK-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts); none — gap (no MITM/relay or flood suite)                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-HSK-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-HSK-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-HSK-2.T1`. | Partial; permutation audit pending | [test/rpc/initHandshake/InitHandshakeChallenge.test.ts](../../../../../../test/rpc/initHandshake/InitHandshakeChallenge.test.ts)                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-HSK-2.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-HSK-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-HSK-2.T1`. | Partial; permutation audit pending | [test/rpc/initHandshake/InitHandshakeChallenge.test.ts](../../../../../../test/rpc/initHandshake/InitHandshakeChallenge.test.ts)                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

**Signing-oracle abuse.** Handled for cross-protocol reuse. The responder signs _any_ well-formed
32-byte challenge on request — it is, by construction, an oracle for `sign(peer3:init-handshake:v1:X)`.
Domain separation (INV-HSK-2) makes the output unusable as a block signature or in any other protocol
domain, and this is pinned by [test/rpc/initHandshake/InitHandshakeChallenge.test.ts](../../../../../../test/rpc/initHandshake/InitHandshakeChallenge.test.ts).
**Residual, and the core concern:** the oracle is still exploitable _inside_ the handshake domain for a
**relay / reflection (man-in-the-middle) attack**, because the signed message binds nothing but the
challenge:

- **Unit / boundary.** [test/rpc/initHandshake/InitHandshakeChallenge.test.ts](../../../../../../test/rpc/initHandshake/InitHandshakeChallenge.test.ts)
  — domain-separation round-trip, non-collision with block signing (INV-HSK-2 / REQ-HSK-2), and
  casing-invariant message derivation.
- **End-to-end.** [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts):
  handshake completion + profile creation (INV-HSK-1); WebRTC transport upgrade updates the profile in
  place (§3.5); request-time skew disconnect, no-response disconnect, response-timestamp mismatch
  disconnect, undecodable-signature disconnect (INV-HSK-5); duplicate-ack disconnect + blacklist
  (§3.4). Broader authentication / signing-oracle coverage is cross-listed in
  [./README.md](../../../implementation/views/architecture/sdk/rpc/README.md) §12.
- **Known gaps (`none — gap`).**
    - No test exercises the **relay / reflection** vector of §4.1 (no adversarial MITM harness). This is
      the highest-value missing test.
    - No test pins the **pre-profile duplicate-ack** degradation (§4.2 / §5) — i.e. that "blacklist"
      becomes disconnect-only when finalize has not yet created a profile.
    - No pre-guard **flood / rate-limit** test (nothing to test until [OQ-6](../../../specification/open-questions.md)).
    - No test asserting the mutual, two-direction finalization gate (that a single-direction handshake
      does not complete a profile).

## architecture/sdk/rpc/is-fork-disputed.md

## Test traceability

| Permutation       | Behavior                                                                              | Implementation obligations                                                                                                       | Test status                        | Exact test evidence                                                                                                                     | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `REQ-IFD-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-IFD-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-1.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-IFD-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-1.T1.P3` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-IFD-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-1.T1.P4` | before/at/after deadline and maximum honest skew                                      | Verify `REQ-IFD-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-1.T1.P5` | duplicate/replay, every relevant permutation and concurrent delivery                  | Verify `REQ-IFD-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-1.T1.P6` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-IFD-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-IFD-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-2.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-IFD-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-2.T1.P3` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-IFD-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-2.T1.P4` | duplicate/replay, every relevant permutation and concurrent delivery                  | Verify `REQ-IFD-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-2.T1.P5` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-IFD-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-IFD-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts) (timeout case)                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-3.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-IFD-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts) (timeout case)                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-3.T1.P3` | before/at/after deadline and maximum honest skew                                      | Verify `REQ-IFD-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts) (timeout case)                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-3.T1.P4` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-IFD-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts) (timeout case)                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-IFD-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-4.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts) ("building on acknowledged disputed fork") | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-4.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-IFD-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-4.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts) ("building on acknowledged disputed fork") | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-4.T1.P3` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-IFD-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-4.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts) ("building on acknowledged disputed fork") | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-IFD-4.T1.P4` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-IFD-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-IFD-4.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts) ("building on acknowledged disputed fork") | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-IFD-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-IFD-1` through the matching implementation conformance row and the concrete obligations refined from `INV-IFD-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-IFD-1.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `INV-IFD-1` through the matching implementation conformance row and the concrete obligations refined from `INV-IFD-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-IFD-1.T1.P3` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-IFD-1` through the matching implementation conformance row and the concrete obligations refined from `INV-IFD-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-IFD-1.T1.P4` | duplicate/replay, every relevant permutation and concurrent delivery                  | Verify `INV-IFD-1` through the matching implementation conformance row and the concrete obligations refined from `INV-IFD-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-IFD-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-IFD-2` through the matching implementation conformance row and the concrete obligations refined from `INV-IFD-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-IFD-2.T1.P2` | duplicate/replay, every relevant permutation and concurrent delivery                  | Verify `INV-IFD-2` through the matching implementation conformance row and the concrete obligations refined from `INV-IFD-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-IFD-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-IFD-3` through the matching implementation conformance row and the concrete obligations refined from `INV-IFD-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts) (duplicate-round case)                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-IFD-3.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `INV-IFD-3` through the matching implementation conformance row and the concrete obligations refined from `INV-IFD-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts) (duplicate-round case)                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-IFD-3.T1.P3` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-IFD-3` through the matching implementation conformance row and the concrete obligations refined from `INV-IFD-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts) (duplicate-round case)                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-IFD-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-IFD-4` through the matching implementation conformance row and the concrete obligations refined from `INV-IFD-4.T1`. | Missing                            | none — gap (no upgrade-crossing test)                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-IFD-4.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-IFD-4` through the matching implementation conformance row and the concrete obligations refined from `INV-IFD-4.T1`. | Missing                            | none — gap (no upgrade-crossing test)                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

**Handled — replay-as-violation** (REQ-RPC-6 pattern 2, cited as the exemplar in
[./README.md](../../../implementation/views/architecture/sdk/rpc/README.md) §6.7). Responder side: duplicate request → disconnect + blacklist
(stage 3). Requester side: the round runs once per fork (`disputedForks` dedup); a duplicated
`DisputeCommitted` event cannot re-fire it. A replayed _response_ cannot land: correlation
accepts exactly one settlement per `requestId`, late/unknown responses are dropped silently
([./README.md](../../../implementation/views/architecture/sdk/rpc/README.md) §6.5). Evidence:
[test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)
(duplicate-request disconnect; duplicate-round ignore).

- **End-to-end:** [test/e2e/E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts)
  — round fan-out + all-peer acks (Byzantine peer excluded); duplicate-round ignored
  (INV-IFD-3); duplicate incoming request → disconnect (INV-IFD-1); non-responding peer →
  timeout disconnect (§6.4 mechanism); building on an acknowledged disputed fork → disconnect
  (§5); non-disputed-fork claim → disconnect (§6.5).
- **Gaps:** no test for the malformed-payload penalty-free path (§6.1) — `none — gap`; no test
  for the cross-channel query (§6.6) — `none — gap`; no test that a responder's chain-read
  failure gets it blacklisted by the requester (the §6.4/§7 asymmetry) — `none — gap`; no test
  of ack-state survival across the WebRTC transport upgrade (INV-IFD-4) — `none — gap`; the
  timeout test uses a spied disconnect rather than a real silent peer over the full window.

## architecture/sdk/rpc/join-channel.md

## Test traceability

| Permutation       | Behavior                                                                              | Implementation obligations                                                                                                       | Test status                        | Exact test evidence                                                                                                                                 | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `INV-JCS-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-JCS-1` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-1.T1`. | Partial; permutation audit pending | [JoinChannelSignatureRequest.test.ts](../../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts)                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-JCS-1.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-JCS-1` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-1.T1`. | Partial; permutation audit pending | [JoinChannelSignatureRequest.test.ts](../../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts)                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-JCS-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-JCS-2` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-2.T1`. | Partial; permutation audit pending | [E2E-JoinChannelRaceConditions.test.ts](../../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts)                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-JCS-2.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `INV-JCS-2` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-2.T1`. | Partial; permutation audit pending | [E2E-JoinChannelRaceConditions.test.ts](../../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts)                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-JCS-2.T1.P3` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-JCS-2` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-2.T1`. | Partial; permutation audit pending | [E2E-JoinChannelRaceConditions.test.ts](../../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts)                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-JCS-2.T1.P4` | new/existing/removed/slashed participant and concurrent membership change             | Verify `INV-JCS-2` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-2.T1`. | Partial; permutation audit pending | [E2E-JoinChannelRaceConditions.test.ts](../../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts)                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-JCS-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-JCS-3` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-3.T1`. | Partial; permutation audit pending | [JoinChannelSignatureRequest.test.ts](../../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts)                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-JCS-3.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-JCS-3` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-3.T1`. | Partial; permutation audit pending | [JoinChannelSignatureRequest.test.ts](../../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts)                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-JCS-3.T1.P3` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `INV-JCS-3` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-3.T1`. | Partial; permutation audit pending | [JoinChannelSignatureRequest.test.ts](../../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts)                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-JCS-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-JCS-4` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-4.T1`. | Missing                            | none — gap (implied by tests; no direct assertion)                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-JCS-4.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-JCS-4` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-4.T1`. | Missing                            | none — gap (implied by tests; no direct assertion)                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-JCS-5.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-JCS-5` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-5.T1`. | Missing                            | none — gap (structural)                                                                                                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-JCS-5.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-JCS-5` through the matching implementation conformance row and the concrete obligations refined from `INV-JCS-5.T1`. | Missing                            | none — gap (structural)                                                                                                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-JCS-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-JCS-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-JCS-1.T1`. | Partial; permutation audit pending | [JoinChannelSignatureRequest.test.ts](../../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts); none — gap (no adversarial fuzz) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-JCS-1.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-JCS-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-JCS-1.T1`. | Partial; permutation audit pending | [JoinChannelSignatureRequest.test.ts](../../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts); none — gap (no adversarial fuzz) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-JCS-1.T1.P3` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-JCS-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-JCS-1.T1`. | Partial; permutation audit pending | [JoinChannelSignatureRequest.test.ts](../../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts); none — gap (no adversarial fuzz) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-JCS-1.T1.P4` | before/at/after deadline and maximum honest skew                                      | Verify `REQ-JCS-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-JCS-1.T1`. | Partial; permutation audit pending | [JoinChannelSignatureRequest.test.ts](../../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts); none — gap (no adversarial fuzz) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-JCS-1.T1.P5` | new/existing/removed/slashed participant and concurrent membership change             | Verify `REQ-JCS-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-JCS-1.T1`. | Partial; permutation audit pending | [JoinChannelSignatureRequest.test.ts](../../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts); none — gap (no adversarial fuzz) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-JCS-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-JCS-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-JCS-2.T1`. | Missing                            | none — gap                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-JCS-2.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-JCS-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-JCS-2.T1`. | Missing                            | none — gap                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-JCS-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-JCS-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-JCS-3.T1`. | Missing                            | none — gap ([OQ-34](../../../specification/open-questions.md), [OQ-6](../../../specification/open-questions.md))                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-JCS-3.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-JCS-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-JCS-3.T1`. | Missing                            | none — gap ([OQ-34](../../../specification/open-questions.md), [OQ-6](../../../specification/open-questions.md))                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-JCS-3.T1.P3` | new/existing/removed/slashed participant and concurrent membership change             | Verify `REQ-JCS-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-JCS-3.T1`. | Missing                            | none — gap ([OQ-34](../../../specification/open-questions.md), [OQ-6](../../../specification/open-questions.md))                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-JCS-3.T1.P4` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-JCS-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-JCS-3.T1`. | Missing                            | none — gap ([OQ-34](../../../specification/open-questions.md), [OQ-6](../../../specification/open-questions.md))                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

A Byzantine peer sends arbitrary bytes. Decode failure (stage 2) throws and is reported as a
request error without touching state. A structurally valid but forged request — signature not
matching the participant, or the sender not the participant — is rejected at stage 3
(`invalid participant signature`). Channel/deadline/fork/snapshot mismatches are rejected at stages
4–7. Handled by the REQ-RPC-2 validation chain in `signJoinRequest`; verified by
[test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts](../../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts).

- **Unit / boundary:**
  [test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts](../../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts)
  exercises: valid request signs the exact join and returns 2 threshold signatures; fail-fast when a
  threshold transport is missing; and forced-inbound-join fallback. Covers INV-JCS-1..4 on the happy
  and missing-transport paths.
- **End-to-end:**
  [test/e2e/E2E-JoinChannelRaceConditions.test.ts](../../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts)
  (pinned-snapshot races, stale snapshot/fork → `RaceCondition*` revert + SDK abort),
  [test/e2e/E2E-ForceJoinDispute.test.ts](../../../../../../test/e2e/E2E-ForceJoinDispute.test.ts)
  (deposit-without-inclusion → force-join dispute),
  [test/e2e/E2E-ParticipantLifecycle.test.ts](../../../../../../test/e2e/E2E-ParticipantLifecycle.test.ts)
  (top-up of existing participant). Contract-side threshold checks:
  [test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts](../../../../../../test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts).
- **Named gaps (`none — gap`):**
    - No test asserts the **penalty-free error probing** consequence (§4.5) or pins it as intended
      behavior vs. a rate-limit target (OQ-34).
    - No adversarial-fuzz test of `requestJoinSignature` payloads at the envelope boundary (shared
      gap with REQ-RPC-2 in [./README.md](../../../implementation/views/architecture/sdk/rpc/README.md) §12).
    - No test pins the SDK union vs. contract threshold equivalence (§2 note).
    - No test covers a **concurrent-join** double-collection race at the RPC layer (§4.4) beyond the
      on-chain race guard.
    - No test asserts a **replayed** join signature is (or is not) accepted across deadline windows or
      domains (§4.3, OQ-29).

## architecture/sdk/rpc/open-channel-negotiation.md

## Test traceability

| Permutation       | Behavior                                                                              | Implementation obligations                                                                                                       | Test status                        | Exact test evidence                                                                                                                                                                         | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `REQ-OCN-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-OCN-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-OCN-1.T1`. | Missing                            | none — gap                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-OCN-1.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-OCN-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-OCN-1.T1`. | Missing                            | none — gap                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-OCN-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-OCN-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-OCN-2.T1`. | Partial; permutation audit pending | [test/rpc/openChannelNegotiation/OpenChannelProposal.test.ts](../../../../../../test/rpc/openChannelNegotiation/OpenChannelProposal.test.ts) (comparison only; service path untested — gap) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-OCN-2.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-OCN-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-OCN-2.T1`. | Partial; permutation audit pending | [test/rpc/openChannelNegotiation/OpenChannelProposal.test.ts](../../../../../../test/rpc/openChannelNegotiation/OpenChannelProposal.test.ts) (comparison only; service path untested — gap) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-OCN-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-OCN-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-OCN-3.T1`. | Missing                            | none — gap                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-OCN-3.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-OCN-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-OCN-3.T1`. | Missing                            | none — gap                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-OCN-3.T1.P3` | before/at/after deadline and maximum honest skew                                      | Verify `REQ-OCN-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-OCN-3.T1`. | Missing                            | none — gap                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-OCN-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-OCN-1` through the matching implementation conformance row and the concrete obligations refined from `INV-OCN-1.T1`. | Missing                            | none — gap                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-OCN-1.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-OCN-1` through the matching implementation conformance row and the concrete obligations refined from `INV-OCN-1.T1`. | Missing                            | none — gap                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-OCN-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-OCN-2` through the matching implementation conformance row and the concrete obligations refined from `INV-OCN-2.T1`. | Missing                            | none — gap                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-OCN-2.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-OCN-2` through the matching implementation conformance row and the concrete obligations refined from `INV-OCN-2.T1`. | Missing                            | none — gap                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-OCN-2.T1.P3` | zero, exact balance/boundary, one beyond, maximum value and conservation              | Verify `INV-OCN-2` through the matching implementation conformance row and the concrete obligations refined from `INV-OCN-2.T1`. | Missing                            | none — gap                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-OCN-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-OCN-3` through the matching implementation conformance row and the concrete obligations refined from `INV-OCN-3.T1`. | Partial; permutation audit pending | [test/rpc/openChannelNegotiation/OpenChannelProposal.test.ts](../../../../../../test/rpc/openChannelNegotiation/OpenChannelProposal.test.ts) (comparison half); none — gap (recovery half)  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-OCN-3.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-OCN-3` through the matching implementation conformance row and the concrete obligations refined from `INV-OCN-3.T1`. | Partial; permutation audit pending | [test/rpc/openChannelNegotiation/OpenChannelProposal.test.ts](../../../../../../test/rpc/openChannelNegotiation/OpenChannelProposal.test.ts) (comparison half); none — gap (recovery half)  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-OCN-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-OCN-4` through the matching implementation conformance row and the concrete obligations refined from `INV-OCN-4.T1`. | Missing                            | none — gap                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-OCN-4.T1.P2` | before/at/after deadline and maximum honest skew                                      | Verify `INV-OCN-4` through the matching implementation conformance row and the concrete obligations refined from `INV-OCN-4.T1`. | Missing                            | none — gap                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

- **Existing:**
  [test/rpc/openChannelNegotiation/OpenChannelProposal.test.ts](../../../../../../test/rpc/openChannelNegotiation/OpenChannelProposal.test.ts)
  pins `getOpenChannelProposalMismatch` (INV-OCN-3's comparison half): tampered balance
  amount/data, participant set, channelId, `isAtomic`, non-empty `data`, past and far-future
  deadlines, case-insensitive address matching.
- **Gaps:** no test instantiates the service or drives the endpoints over a wired custom root
  (the entire §4 flow, both roles); no test covers signature-recovery rejection, the
  unnegotiated-proposal blacklist, slot busy/timeout behavior, `openProposal` replay, or
  malformed `amount` values; no e2e negotiates and opens a channel through this service.
- **Risk-first order if wiring is decided:** malformed-payload matrix at each endpoint
  (REQ-RPC-2), proposal replay and double-`open` gas behavior, slot-occupation contention, then
  a two-node e2e open.

## architecture/sdk/rpc/spectate.md

## Test traceability

| Permutation       | Behavior                                                                              | Implementation obligations                                                                                                       | Test status                        | Exact test evidence                                                                                                                                                                                                                                            | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `INV-SPC-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-SPC-1` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-1.T1`. | Partial; permutation audit pending | [E2E-Spectate.test.ts](../../../../../../test/e2e/E2E-Spectate.test.ts), [E2E-SpectateStaleProofGuard.test.ts](../../../../../../test/e2e/E2E-SpectateStaleProofGuard.test.ts), [SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-1.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-SPC-1` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-1.T1`. | Partial; permutation audit pending | [E2E-Spectate.test.ts](../../../../../../test/e2e/E2E-Spectate.test.ts), [E2E-SpectateStaleProofGuard.test.ts](../../../../../../test/e2e/E2E-SpectateStaleProofGuard.test.ts), [SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-SPC-2` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-2.T1`. | Missing                            | none — gap (implicit; no direct assertion)                                                                                                                                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-2.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-SPC-2` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-2.T1`. | Missing                            | none — gap (implicit; no direct assertion)                                                                                                                                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-SPC-3` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-3.T1`. | Missing                            | none — gap                                                                                                                                                                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-3.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-SPC-3` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-3.T1`. | Missing                            | none — gap                                                                                                                                                                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-SPC-4` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-4.T1`. | Partial; permutation audit pending | [E2E-SpectatingAbortDoS.test.ts](../../../../../../test/e2e/E2E-SpectatingAbortDoS.test.ts), [E2E-Spectate.test.ts](../../../../../../test/e2e/E2E-Spectate.test.ts)                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-4.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `INV-SPC-4` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-4.T1`. | Partial; permutation audit pending | [E2E-SpectatingAbortDoS.test.ts](../../../../../../test/e2e/E2E-SpectatingAbortDoS.test.ts), [E2E-Spectate.test.ts](../../../../../../test/e2e/E2E-Spectate.test.ts)                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-4.T1.P3` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `INV-SPC-4` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-4.T1`. | Partial; permutation audit pending | [E2E-SpectatingAbortDoS.test.ts](../../../../../../test/e2e/E2E-SpectatingAbortDoS.test.ts), [E2E-Spectate.test.ts](../../../../../../test/e2e/E2E-Spectate.test.ts)                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-5.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-SPC-5` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-5.T1`. | Partial; permutation audit pending | [E2E-Spectate.test.ts](../../../../../../test/e2e/E2E-Spectate.test.ts)                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-5.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `INV-SPC-5` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-5.T1`. | Partial; permutation audit pending | [E2E-Spectate.test.ts](../../../../../../test/e2e/E2E-Spectate.test.ts)                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-5.T1.P3` | zero, exact balance/boundary, one beyond, maximum value and conservation              | Verify `INV-SPC-5` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-5.T1`. | Partial; permutation audit pending | [E2E-Spectate.test.ts](../../../../../../test/e2e/E2E-Spectate.test.ts)                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-6.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-SPC-6` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-6.T1`. | Partial; permutation audit pending | [E2E-Spectate.test.ts](../../../../../../test/e2e/E2E-Spectate.test.ts)                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-SPC-6.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-SPC-6` through the matching implementation conformance row and the concrete obligations refined from `INV-SPC-6.T1`. | Partial; permutation audit pending | [E2E-Spectate.test.ts](../../../../../../test/e2e/E2E-Spectate.test.ts)                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SPC-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-SPC-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-SPC-1.T1`. | Partial; permutation audit pending | [SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts)                                                                                                                                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SPC-1.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-SPC-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-SPC-1.T1`. | Partial; permutation audit pending | [SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts)                                                                                                                                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SPC-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-SPC-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-SPC-2.T1`. | Missing                            | none — gap ([DEF-5](../../../specification/open-questions.md))                                                                                                                                                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SPC-2.T1.P2` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-SPC-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-SPC-2.T1`. | Missing                            | none — gap ([DEF-5](../../../specification/open-questions.md))                                                                                                                                                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SPC-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-SPC-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-SPC-3.T1`. | Missing                            | none — gap (decision pending)                                                                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SPC-3.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-SPC-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-SPC-3.T1`. | Missing                            | none — gap (decision pending)                                                                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SPC-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-SPC-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-SPC-4.T1`. | Missing                            | none — gap ([OQ-6](../../../specification/open-questions.md))                                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-SPC-4.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-SPC-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-SPC-4.T1`. | Missing                            | none — gap ([OQ-6](../../../specification/open-questions.md))                                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

Handled by the requester-side verification chain; all contract checks run as `staticCall`/local-EVM
simulations, so verification itself sends no transaction. Verified by
[test/e2e/E2E-Spectate.test.ts](../../../../../../test/e2e/E2E-Spectate.test.ts),
[test/e2e/E2E-SpectateStaleProofGuard.test.ts](../../../../../../test/e2e/E2E-SpectateStaleProofGuard.test.ts),
[test/unit/SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts).

A malicious responder that repeatedly serves just-barely-invalid payloads forces the spectator to
abort repeatedly. For a fresh spectator this stops the node (fail-closed, §3.4) rather than looping;
for a participant recovering, only the bad peer is cut. The one-in-flight rule prevents self-inflicted
concurrent-sync amplification. Verified by
[test/e2e/E2E-SpectatingAbortDoS.test.ts](../../../../../../test/e2e/E2E-SpectatingAbortDoS.test.ts).
Residual overlaps with §4.4 (request frequency), tracked by OQ-6.

- **Unit:** [test/unit/SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts) —
  `generateSyncPayload` recovers committed-but-locally-missing disputes before proving; declines a
  disputed fork even when all dispute events are locally suppressed (INV-SPC-1 responder side).
- **End-to-end:**
  [test/e2e/E2E-Spectate.test.ts](../../../../../../test/e2e/E2E-Spectate.test.ts) (honest sync;
  balance invariant runs inside every successful sync),
  [test/e2e/E2E-SpectateStaleProofGuard.test.ts](../../../../../../test/e2e/E2E-SpectateStaleProofGuard.test.ts)
  (stale-proof rejection, step 6),
  [test/e2e/E2E-SpectatingAbortDoS.test.ts](../../../../../../test/e2e/E2E-SpectatingAbortDoS.test.ts)
  (repeated-abort DoS, §4.6),
  [test/e2e/E2E-SpectatorStateProofPersistence.test.ts](../../../../../../test/e2e/E2E-SpectatorStateProofPersistence.test.ts)
  (persistence of proved state, step 13),
  [test/e2e/E2E-ByzantineErrorAttribution.test.ts](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts)
  (error attribution across the boundary).
- **Named gaps (`none — gap`):**
    - No test distinguishes **honest-unavailable from malicious-withholding** on the request path
      (DEF-5, §4.3) — by construction, since the code does not distinguish them.
    - No test covers the **honest lagging responder → unprovable → blacklist** griefing path (§4.2).
    - No **rate-limit / proof-flood** test (§4.4) — nothing to test until OQ-6 is implemented.
    - No test pins the **spectate-skips-invariant** residual (§4.1 / OQ-19) — out of this service's
      scope but noted for the invariant's overall coverage.

## architecture/sdk/rpc/state-transition.md

## Test traceability

| Permutation       | Behavior                                                                              | Implementation obligations                                                                                                       | Test status                        | Exact test evidence                                                                                                                                                                   | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `REQ-STS-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-STS-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-STS-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-BlockQueueManager.test.ts](../../../../../../test/e2e/E2E-BlockQueueManager.test.ts)                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-STS-1.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-STS-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-STS-1.T1`. | Partial; permutation audit pending | [test/e2e/E2E-BlockQueueManager.test.ts](../../../../../../test/e2e/E2E-BlockQueueManager.test.ts)                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-STS-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-STS-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-STS-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-BlockQueueManager.test.ts](../../../../../../test/e2e/E2E-BlockQueueManager.test.ts) (wrong-channel and forged-signature cuts)                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-STS-2.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-STS-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-STS-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-BlockQueueManager.test.ts](../../../../../../test/e2e/E2E-BlockQueueManager.test.ts) (wrong-channel and forged-signature cuts)                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-STS-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-STS-1` through the matching implementation conformance row and the concrete obligations refined from `INV-STS-1.T1`. | Partial; permutation audit pending | via REQ-BCP-3/INV-RPC-5 evidence ([../block-confirmation-pipeline.md](../../../implementation/views/architecture/sdk/block-confirmation-pipeline.md) §12); dedicated test: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-STS-1.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-STS-1` through the matching implementation conformance row and the concrete obligations refined from `INV-STS-1.T1`. | Partial; permutation audit pending | via REQ-BCP-3/INV-RPC-5 evidence ([../block-confirmation-pipeline.md](../../../implementation/views/architecture/sdk/block-confirmation-pipeline.md) §12); dedicated test: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-STS-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-STS-2` through the matching implementation conformance row and the concrete obligations refined from `INV-STS-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-ByzantineErrorAttribution.test.ts](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts)                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-STS-2.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-STS-2` through the matching implementation conformance row and the concrete obligations refined from `INV-STS-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-ByzantineErrorAttribution.test.ts](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts)                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-STS-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-STS-3` through the matching implementation conformance row and the concrete obligations refined from `INV-STS-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-BlockQueueManager.test.ts](../../../../../../test/e2e/E2E-BlockQueueManager.test.ts)                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-STS-3.T1.P2` | static review of every named alternative, omitted category and changed assumption     | Verify `INV-STS-3` through the matching implementation conformance row and the concrete obligations refined from `INV-STS-3.T1`. | Partial; permutation audit pending | [test/e2e/E2E-BlockQueueManager.test.ts](../../../../../../test/e2e/E2E-BlockQueueManager.test.ts)                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

Evidence: [test/e2e/E2E-BlockQueueManager.test.ts](../../../../../../test/e2e/E2E-BlockQueueManager.test.ts)
("ingest rejects a block confirmation with a forged author signature").

**Handled with a bounded residual.** A peer can take a genuine confirmation and append garbage
or non-participant signatures. Merge paths compute the stray set and punish **exactly the
transports that supplied those signatures** — `signatureSources` maps each signature to its
suppliers; `notAllSingersAreParticipants` →
[`BlockQueueManager.disconnectPeersForSignatures`](../../../../../../src/stateManager/BlockQueueManager.ts)
— honest relayers of the clean copy are untouched, and the strays are stripped rather than the
block rejected. The 128-tracked-source cap bounds attribution memory per entry; overflow sets a
marker and **never** rejects a later valid copy (INV-BCP-4), so cap exhaustion cannot be
weaponized to suppress an honest block. Evidence:
[test/e2e/E2E-ByzantineErrorAttribution.test.ts](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts),
[test/e2e/E2E-BlockQueueManager.test.ts](../../../../../../test/e2e/E2E-BlockQueueManager.test.ts)
("strays stripped, supplier blacklisted"). Residual: for a **queued** (not yet stored) entry,
junk signatures accumulate unvalidated in the entry's signature set until execution or timeout;
the signature _set_ itself has no explicit count cap — per-frame growth is bounded by the 16 MiB
cap and the entry dies at `agreementTime`, so total growth is again a rate question →
[OQ-6](../../../specification/open-questions.md).

- **End-to-end (primary evidence):**
  [test/e2e/E2E-BlockQueueManager.test.ts](../../../../../../test/e2e/E2E-BlockQueueManager.test.ts)
  — forged author signature rejected; wrong-channel cut; outsider-authored block cuts relayer
  and author; future-block eviction without punishment; stored-merge stray-stripping; the
  wrong-fork suite (local reduction at ingest, unknown-fork sync probes and their punishments).
  [test/e2e/E2E-StateTransition.test.ts](../../../../../../test/e2e/E2E-StateTransition.test.ts) —
  happy-path gossip rounds, echo dedup.
  [test/e2e/E2E-ByzantineErrorAttribution.test.ts](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts)
  — signature-source attribution.
- **Gaps:** no unit test exercises `StateTransitionRpcMethods` directly (guard bypass attempt,
  missing-`peerAddress` branch, verdict mapping in isolation) — `none — gap`; no flood/rate
  test (nothing to enforce yet, [OQ-6](../../../specification/open-questions.md)); no test pinning the §4.1
  local-error-blacklists-sender behavior (deliberately unpinned until the defect candidate is
  decided); no adversarial fuzz of the struct shape at this endpoint (model-doc-wide gap,
  REQ-RPC-2 row).

## architecture/sdk/rpc/webrtc-setup.md

## Test traceability

| Permutation        | Behavior                                                                              | Implementation obligations                                                                                                         | Test status                        | Exact test evidence                                                                                                                                                              | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ------------------ | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `INV-WRTC-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-WRTC-1` through the matching implementation conformance row and the concrete obligations refined from `INV-WRTC-1.T1`. | Missing                            | none — gap (no payload-address-spoof test)                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-WRTC-1.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-WRTC-1` through the matching implementation conformance row and the concrete obligations refined from `INV-WRTC-1.T1`. | Missing                            | none — gap (no payload-address-spoof test)                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-WRTC-2.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-WRTC-2` through the matching implementation conformance row and the concrete obligations refined from `INV-WRTC-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts) (upgrade runs post-handshake); none — gap (no pre-handshake signaling-rejection test) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-WRTC-2.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-WRTC-2` through the matching implementation conformance row and the concrete obligations refined from `INV-WRTC-2.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts) (upgrade runs post-handshake); none — gap (no pre-handshake signaling-rejection test) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-WRTC-3.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-WRTC-3` through the matching implementation conformance row and the concrete obligations refined from `INV-WRTC-3.T1`. | Missing                            | none — gap (no malformed-payload silent-ignore test)                                                                                                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-WRTC-3.T1.P2` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `INV-WRTC-3` through the matching implementation conformance row and the concrete obligations refined from `INV-WRTC-3.T1`. | Missing                            | none — gap (no malformed-payload silent-ignore test)                                                                                                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-WRTC-4.T1.P1` | valid case and direct invalid/opposite                                                | Verify `INV-WRTC-4` through the matching implementation conformance row and the concrete obligations refined from `INV-WRTC-4.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts) (WebRTC upgrade)                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-WRTC-4.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-WRTC-4` through the matching implementation conformance row and the concrete obligations refined from `INV-WRTC-4.T1`. | Partial; permutation audit pending | [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts) (WebRTC upgrade)                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-WRTC-1.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-WRTC-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-WRTC-1.T1`. | Missing                            | none — gap (no adversarial SDP/ICE suite; dependency assumption not yet in trust-model)                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-WRTC-1.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-WRTC-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-WRTC-1.T1`. | Missing                            | none — gap (no adversarial SDP/ICE suite; dependency assumption not yet in trust-model)                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

- **Unit / component.** [test/utils/WebRTCTransport.test.ts](../../../../../../test/utils/WebRTCTransport.test.ts)
  — the transport queues sends on a connecting channel and flushes on open, starts the handshake exactly
  once on open (INV-WRTC linkage to handshake), sends over an open channel, drops sends on a closed
  channel. [test/utils/WebRTCWorkerBridgeConnectionFactory.test.ts](../../../../../../test/utils/WebRTCWorkerBridgeConnectionFactory.test.ts)
  — the worker-bridge factory constructs proxy channels, routes state/ICE events to callbacks, and
  rejects in-flight requests on bridge disposal.
- **End-to-end.** [test/e2e/E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts)
  "should update existing profile transport on WebRTC upgrade" drives a real offer via `initiateWebRTC`
  and asserts the profile is upgraded in place from `HOLEPUNCH` to `WEBRTC` (INV-WRTC-4 offerer path,
  and the §1 upgrade→profile-replacement flow). Transport-mode coverage:
  [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts).
- **Known gaps (`none — gap`).**
    - No test feeds a **malicious/malformed SDP or ICE** payload to `onOffer/onAnswer/onIceCandidate` and
      asserts silent-ignore (INV-WRTC-3) and that no cross-identity effect occurs (INV-WRTC-1).
    - No test asserts a signaling frame **cannot key another peer's address** via the payload (INV-WRTC-1).
    - No test for **offer-churn / connection-reset** resource behavior (§4.4).
    - No test constrains **ICE candidate targets** (§4.2) — currently unconstrained by design.

## architecture/sdk/runtime-and-concurrency.md

## Test traceability

| Permutation        | Behavior                                                                              | Implementation obligations                                                                                                         | Test status                        | Exact test evidence                                                                                                                                                                                                                                                                                                                         | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ------------------ | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `REQ-RUN-1.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-1.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts) (all 4 combos)                                                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-1.T1.P2`  | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-RUN-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-1.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts) (all 4 combos)                                                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-2.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-2.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts) (worker mode) — no shared-memory negative test possible; structural                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-2.T1.P2`  | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-RUN-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-2.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts) (worker mode) — no shared-memory negative test possible; structural                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-3.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-3.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts)                                                                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-3.T1.P2`  | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-RUN-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-3.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts)                                                                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-4.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-4.T1`.   | Partial; permutation audit pending | partial — [test/e2e/E2E-RuntimeContractEvents.test.ts](../../../../../../test/e2e/E2E-RuntimeContractEvents.test.ts), [test/e2e/E2E-WorkerShutdown.test.ts](../../../../../../test/e2e/E2E-WorkerShutdown.test.ts); ordering/serialization-loss contracts: none — gap (§3.2, §3.6 open questions)                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-4.T1.P2`  | duplicate/replay, every relevant permutation and concurrent delivery                  | Verify `REQ-RUN-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-4.T1`.   | Partial; permutation audit pending | partial — [test/e2e/E2E-RuntimeContractEvents.test.ts](../../../../../../test/e2e/E2E-RuntimeContractEvents.test.ts), [test/e2e/E2E-WorkerShutdown.test.ts](../../../../../../test/e2e/E2E-WorkerShutdown.test.ts); ordering/serialization-loss contracts: none — gap (§3.2, §3.6 open questions)                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-5.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-5.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts)                                                                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-5.T1.P2`  | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-RUN-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-5.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts)                                                                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-6.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-6.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts); bigint-cross tests via [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts) (REQ-RPC-4); serialization-loss contract: none — gap                                                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-6.T1.P2`  | zero, exact balance/boundary, one beyond, maximum value and conservation              | Verify `REQ-RUN-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-6.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts); bigint-cross tests via [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts) (REQ-RPC-4); serialization-loss contract: none — gap                                                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-6.T1.P3`  | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-RUN-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-6.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts); bigint-cross tests via [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts) (REQ-RPC-4); serialization-loss contract: none — gap                                                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-7.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-7.T1`.   | Missing                            | none — gap (flags off by default; no benchmark gates them; no performance target — §6 open question)                                                                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-7.T1.P2`  | duplicate/replay, every relevant permutation and concurrent delivery                  | Verify `REQ-RUN-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-7.T1`.   | Missing                            | none — gap (flags off by default; no benchmark gates them; no performance target — §6 open question)                                                                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RUN-1.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-RUN-1` through the matching implementation conformance row and the concrete obligations refined from `INV-RUN-1.T1`.   | Partial; permutation audit pending | [block-confirmation-pipeline.md](../../../implementation/views/architecture/sdk/block-confirmation-pipeline.md) §12 (REQ-BCP-3/4); executor-under-mutex race test: none — gap                                                                                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RUN-1.T1.P2`  | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-RUN-1` through the matching implementation conformance row and the concrete obligations refined from `INV-RUN-1.T1`.   | Partial; permutation audit pending | [block-confirmation-pipeline.md](../../../implementation/views/architecture/sdk/block-confirmation-pipeline.md) §12 (REQ-BCP-3/4); executor-under-mutex race test: none — gap                                                                                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RUN-2.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-RUN-2` through the matching implementation conformance row and the concrete obligations refined from `INV-RUN-2.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts)                                                                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RUN-2.T1.P2`  | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-RUN-2` through the matching implementation conformance row and the concrete obligations refined from `INV-RUN-2.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts)                                                                                                                                                                                                                                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RUN-3.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-RUN-3` through the matching implementation conformance row and the concrete obligations refined from `INV-RUN-3.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-WorkerShutdown.test.ts](../../../../../../test/e2e/E2E-WorkerShutdown.test.ts); host-construction-failure rejection: none — gap                                                                                                                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RUN-3.T1.P2`  | malformed and adversarial input, partial failure, retry and recovery                  | Verify `INV-RUN-3` through the matching implementation conformance row and the concrete obligations refined from `INV-RUN-3.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-WorkerShutdown.test.ts](../../../../../../test/e2e/E2E-WorkerShutdown.test.ts); host-construction-failure rejection: none — gap                                                                                                                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RUN-4.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-RUN-4` through the matching implementation conformance row and the concrete obligations refined from `INV-RUN-4.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts) (host-owned random signer); cross-ref [architecture.md](../../../implementation/views/architecture/sdk/architecture.md) INV-SDK-2                                                                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RUN-4.T1.P2`  | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-RUN-4` through the matching implementation conformance row and the concrete obligations refined from `INV-RUN-4.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts) (host-owned random signer); cross-ref [architecture.md](../../../implementation/views/architecture/sdk/architecture.md) INV-SDK-2                                                                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-8.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-8` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-8.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts) (loopback + peer-addressed), [test/e2e/E2E-PingService.test.ts](../../../../../../test/e2e/E2E-PingService.test.ts); standing use across [test/e2e](../../../../../../test/e2e) / [test/unit](../../../../../../test/unit) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-8.T1.P2`  | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-RUN-8` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-8.T1`.   | Partial; permutation audit pending | [test/e2e/E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts) (loopback + peer-addressed), [test/e2e/E2E-PingService.test.ts](../../../../../../test/e2e/E2E-PingService.test.ts); standing use across [test/e2e](../../../../../../test/e2e) / [test/unit](../../../../../../test/unit) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-9.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-9.T1`.   | Partial; permutation audit pending | implicit across the suite (a non-serializable return fails the request); no dedicated test — partial                                                                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-9.T1.P2`  | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-RUN-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-9.T1`.   | Partial; permutation audit pending | implicit across the suite (a non-serializable return fails the request); no dedicated test — partial                                                                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-10.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-10.T1`. | Missing                            | none — gap (no test asserts network reachability either way; §11.4 open question, security-relevant, divergence class decision pending)                                                                                                                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-10.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-RUN-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-10.T1`. | Missing                            | none — gap (no test asserts network reachability either way; §11.4 open question, security-relevant, divergence class decision pending)                                                                                                                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-11.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-11.T1`. | Partial; permutation audit pending | [test/scripts](../../../../../../test/scripts) (runner machinery); isolation properties themselves: none — gap (default is one shared chain per machine — §11.5 open question)                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-11.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-RUN-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-11.T1`. | Partial; permutation audit pending | [test/scripts](../../../../../../test/scripts) (runner machinery); isolation properties themselves: none — gap (default is one shared chain per machine — §11.5 open question)                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-11.T1.P3` | new/existing/removed/slashed participant and concurrent membership change             | Verify `REQ-RUN-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-11.T1`. | Partial; permutation audit pending | [test/scripts](../../../../../../test/scripts) (runner machinery); isolation properties themselves: none — gap (default is one shared chain per machine — §11.5 open question)                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-12.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-12` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-12.T1`. | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts) (same assertions per mode, no cross-mode comparison); cross-mode diff and multi-peer scenarios: none — gap                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-12.T1.P2` | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-RUN-12` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-12.T1`. | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts) (same assertions per mode, no cross-mode comparison); cross-mode diff and multi-peer scenarios: none — gap                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-13.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-13` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-13.T1`. | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts) (all four combinations); default-flip readiness and worker-capability fallback: none — gap                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-13.T1.P2` | before/at/after deadline and maximum honest skew                                      | Verify `REQ-RUN-13` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-13.T1`. | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts) (all four combinations); default-flip readiness and worker-capability fallback: none — gap                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-14.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-14` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-14.T1`. | Missing                            | none — gap (no device-envelope or memory-ceiling benchmark; blocks REQ-RUN-13)                                                                                                                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-14.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-RUN-14` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-14.T1`. | Missing                            | none — gap (no device-envelope or memory-ceiling benchmark; blocks REQ-RUN-13)                                                                                                                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-15.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-RUN-15` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-15.T1`. | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts) (partial: per-mode assertions only)                                                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RUN-15.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-RUN-15` through the matching implementation conformance row and the concrete obligations refined from `REQ-RUN-15.T1`. | Partial; permutation audit pending | [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts) (partial: per-mode assertions only)                                                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RUN-5.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-RUN-5` through the matching implementation conformance row and the concrete obligations refined from `INV-RUN-5.T1`.   | Partial; permutation audit pending | the suite's white-box assertions; structural — no dedicated test                                                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RUN-5.T1.P2`  | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `INV-RUN-5` through the matching implementation conformance row and the concrete obligations refined from `INV-RUN-5.T1`.   | Partial; permutation audit pending | the suite's white-box assertions; structural — no dedicated test                                                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RUN-6.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-RUN-6` through the matching implementation conformance row and the concrete obligations refined from `INV-RUN-6.T1`.   | Missing                            | none — gap (structural convention, unenforced)                                                                                                                                                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RUN-6.T1.P2`  | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-RUN-6` through the matching implementation conformance row and the concrete obligations refined from `INV-RUN-6.T1`.   | Missing                            | none — gap (structural convention, unenforced)                                                                                                                                                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

- **Inline/worker equivalence across all four combos:**
  [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts)
  runs `{RUN_SDK_IN_THREAD} × {VM_DEDICATED_THREAD}` and round-trips contract
  calls in each — the direct evidence that observable behavior is transport
  neutral (REQ-RUN-1). It also covers the host-owned random signer path
  (REQ-RUN-3 / INV-RUN-4).
- **Event surface across the port:**
  [test/e2e/E2E-RuntimeContractEvents.test.ts](../../../../../../test/e2e/E2E-RuntimeContractEvents.test.ts)
  (bus events and contract events crossing the boundary).
- **Worker lifecycle/disposal:**
  [test/e2e/E2E-WorkerShutdown.test.ts](../../../../../../test/e2e/E2E-WorkerShutdown.test.ts)
  (drain-to-exit teardown, INV-RUN-3 / §3.5).
- **Custom root across the boundary (manifest-not-instance serialization):**
  [test/e2e/E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts).
- **Runner context.** The serializable-port design is what lets the parallel and
  distributed runners drive many peers per machine (§11);
  [../reference/configuration.md](../../../implementation/views/operations/configuration.md) documents
  `yarn test:parallel` (canonical gate), `test:parallel:distributed`,
  `:server`, `:prepare`.
- **Gaps.** No test pins the cross-kind message-ordering guarantee (§3.2); none
  asserts bus-event arg-loss or indexed-filter behavior as a contract (§3.6);
  none benchmarks worker startup/transfer cost as a gate (§6); no
  performance-target or device-envelope test exists (there is no target to
  assert — §6 open question).

The serializable-port design is also a **test-control** capability, not only a
deployment one. Because a peer is driven entirely through serialized interfaces,
one controller can operate peers the same way whether they run **inline, in local
workers, or on another machine**, and can control **many peers without coupling to
their internal memory** — `P2pInstance.getStateManager()` throws precisely to
forbid that coupling ([architecture.md](../../../implementation/views/architecture/sdk/architecture.md) INV-SDK-1). The
consequence for testing is concrete: when the SDK moved behind the runtime port,
a harness could no longer reach `peer.stateManager` by reference, so every
white-box operation a test needs had to become a _message_
([`HarnessControlRpc`](../../../../../../test/fixtures/customRpc/harnessControl/HarnessControlRpc.ts)
header comment).

- **Registration.** The harness ships the root as a `customRpcManifest` into
  `p2pSetup` like any integrator would:
  [`PeerTestHarness.resolveHarnessRpcManifest`](../../../../../../test/fixtures/PeerTestHarness.ts)
  resolves the module specifier
  (`customRpc/harnessControl/HarnessControlRpc.{ts|js}`, extension chosen from
  `__filename` so `dist` consumers work) and passes it to
  `EvmStateMachine.p2pSetup`. A test that needs its own services supplies a
  manifest whose class **extends** `HarnessControlRpc`, so every peer keeps the
  full control surface (e.g.
  [`PingPongRpc`](../../../../../../test/fixtures/customRpc/PingPongRpcManifest.ts)).
  The manifest-not-instance rule is the §3.6 serialization constraint, not a
  harness choice.
- **Addressing.**
  [`PeerTestHarness.control(peer)`](../../../../../../test/fixtures/PeerTestHarness.ts)
  returns `peer.p2pInstance.hostRpc` typed as
  `RemoteRpcProxyType<HarnessControlRpc>`; a call is written
  `harness.control(peer).<service>.<method>(...).request()`. **No target means
  loopback** — the call executes on that peer's own host through
  [`LoopbackTransport`](../../../../../../src/transport/LoopbackTransport.ts). Passing
  an EVM address relays the same typed call to that peer over its network
  transport, which is how a test drives peer→peer traffic
  ([test/e2e/E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts)
  exercises both forms in one run).
- **Serialization discipline.** Every operation returns structured-clone-safe
  projections — status, hash, height, address, plain struct — never a live
  `Block`, transport, or profile; bigint-bearing protocol structs cross
  `Codec.encode`d and are decoded inside the host endpoint, exactly as the peer
  boundary requires ([rpc/README.md](../../../implementation/views/architecture/sdk/rpc/README.md) §6.3, REQ-RPC-4). This is
  **REQ-RUN-9** and **INV-RUN-5**, and it is why endpoints are named
  `encodedSyncPayload` / `encodedBalanceDelta` / `encodedSnapshot` rather than
  returning the objects.

Eleven services on one root
([`HarnessControlRpc`](../../../../../../test/fixtures/customRpc/harnessControl/HarnessControlRpc.ts)),
each a `Service` + `RpcMethods` pair under
[`test/fixtures/customRpc/harnessControl/services/`](../../../../../../test/fixtures/customRpc/harnessControl/services),
**207 public endpoints** in total. Only endpoints live on the `RpcMethods` class;
accessors and mutable state live on the service, because every `RpcMethods`
member is routable by name at runtime (REQ-RPC-1).

| Service                                                                                                                                           | Endpoints | What it does                                                                                                                                                                                                                                                                                                                                                                                                              | State it touches                                                                                                                                                                             | Return                                                                |
| ------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| `query` ([QueryRpcMethods](../../../../../../test/fixtures/customRpc/harnessControl/services/query/QueryRpcMethods.ts))                           | 55        | Read-only peer state: status, channel/fork id, participants, sync tip, block/snapshot/state lookups, dispute + fraud-proof reads, connection and blacklist state, on-chain reads (`getStateProofVerification`, `getDisputeWindowCommitments`), block-building context, protocol clock.                                                                                                                                    | Reads `StateManager`, storage, `ProfileManager`, chain. No mutation.                                                                                                                         | Serializable projections; encoded structs where bigints are involved. |
| `stub` ([StubRpcMethods](../../../../../../test/fixtures/customRpc/harnessControl/services/stub/StubRpcMethods.ts))                               | 103       | Fault injection and instrumentation: install a test double at a named site (`stubBroadcast`, `stubCalldataPosting`, `stubHandshakeResponse`, `stubSuppressDisputeKill`, …), hold/release/replay events and reduction tasks, record call counts, run white-box `probe*` scenarios. Each `stubX` saves the live original once in a per-service registry; `restoreX` reinstalls it (returns `false` if nothing was stubbed). | Reassigns **instance** members of the live host graph (`localRpc.*`, `eventHandler`, managers). Sites are concrete methods, not free-form path strings, so an SDK rename breaks compilation. | `boolean` / recorded counters / probe results.                        |
| `dispute` ([DisputeRpcMethods](../../../../../../test/fixtures/customRpc/harnessControl/services/dispute/DisputeRpcMethods.ts))                   | 13        | Drive and inspect disputes: `startReduction`/`awaitReduction`, `recoverCommittedDisputes`, `constructDispute`, `getAuditingData`, `getKillPeriod`, plus **tampering** (`stubConstructDispute` with a strategy or a shipped `(dispute,args)=>void` body, `restoreConstructDispute`, `setForceExit`, `plantFreshTimeout`).                                                                                                  | `reductionManager`, `disputeManager`, timeout state; re-signs via `signer`.                                                                                                                  | Encoded `Dispute` / `DisputeConfirmation` / `DisputeAuditingData`.    |
| `handshake` ([HandshakeRpcMethods](../../../../../../test/fixtures/customRpc/harnessControl/services/handshake/HandshakeRpcMethods.ts))           | 13        | White-box driver for authentication and dispute-acknowledgment: initiate a handshake, deliver a crafted request/ack over a chosen transport, sign an arbitrary message hash, query completion, count disputed forks.                                                                                                                                                                                                      | `initHandshakeService`, `isForkDisputedService`, `ProfileManager`, **the host signing key**.                                                                                                 | Booleans, signatures, counts.                                         |
| `byzantine` ([ByzantineRpcMethods](../../../../../../test/fixtures/customRpc/harnessControl/services/byzantine/ByzantineRpcMethods.ts))           | 5         | Block-level faults: sign and broadcast a harness-assembled block, send a crafted confirmation to one peer, apply a transaction to get a real snapshot hash for a forged block, `submitDoubleSignBlock`, `postJunkCalldataOnChain`. Assembly stays client-side; only signing/broadcast needs the host.                                                                                                                     | Signer, storage head, `stateTransitionService` broadcast path, chain.                                                                                                                        | Hashes/heights.                                                       |
| `transition` ([TransitionRpcMethods](../../../../../../test/fixtures/customRpc/harnessControl/services/transition/TransitionRpcMethods.ts))       | 5         | Snapshot posting (`postStateSnapshot`, `…Wait`, `prepareUpdateSnapshotSameFork`), `ingestBlockConfirmation`, and `storeBlock` (persist straight to storage, bypassing the pipeline).                                                                                                                                                                                                                                      | `snapshotUpdateService`, `BlockQueueManager`, block storage, chain.                                                                                                                          | Encoded snapshots / booleans.                                         |
| `spectate` ([SpectateControlRpcMethods](../../../../../../test/fixtures/customRpc/harnessControl/services/spectate/SpectateControlRpcMethods.ts)) | 5         | Spectator flow: generate / apply / persist a sync payload, start a real sync toward a peer, `storeBlockJustPersist`.                                                                                                                                                                                                                                                                                                      | `spectateService`, block storage.                                                                                                                                                            | Encoded `SyncPayload`, hashes, `{shouldAbort}`.                       |
| `network` ([NetworkRpcMethods](../../../../../../test/fixtures/customRpc/harnessControl/services/network/NetworkRpcMethods.ts))                   | 3         | Connectivity control: `connectToChannel` (starts local discovery and wires peers host-side, idempotent), `disconnectAllConnections`, `disconnectPeerByAddress` — partition simulation.                                                                                                                                                                                                                                    | `p2pSigner`, `LocalDiscoveryServer`, open transports.                                                                                                                                        | Booleans / counts.                                                    |
| `balance` ([BalanceRpcMethods](../../../../../../test/fixtures/customRpc/harnessControl/services/balance/BalanceRpcMethods.ts))                   | 3         | Balance math on the peer's diamond state machine: withdrawal delta over a block range, subtract, compare.                                                                                                                                                                                                                                                                                                                 | Outbound-message storage, diamond state machine.                                                                                                                                             | Encoded `Balance` / boolean.                                          |
| `signer` ([SignerRpcMethods](../../../../../../test/fixtures/customRpc/harnessControl/services/signer/SignerRpcMethods.ts))                       | 1         | `registerPeerSigners(secrets)` — push **other peers' private keys** into this host so tamper callbacks can re-sign blocks authored by any participant.                                                                                                                                                                                                                                                                    | Adds `Wallet`s to a host-side registry keyed by address.                                                                                                                                     | `boolean`.                                                            |
| `scenario` ([ScenarioRpcMethods](../../../../../../test/fixtures/customRpc/harnessControl/services/scenario/ScenarioRpcMethods.ts))               | 1         | `exec(fnBody, args)` — rebuild a caller-supplied `(sm, args, modules) => result` source string with `new Function` and run it **with the live `StateManager` injected**. The escape hatch for in-process internals (mutex, validation strategies, local RPC services) that have no endpoint.                                                                                                                              | Anything reachable from `stateManager`.                                                                                                                                                      | Whatever the body returns (must be serializable).                     |

The controller-side wrappers over these endpoints live in
[`test/harness/actions/`](../../../../../../test/harness/actions) and
[`PeerTestHarness.execOnHost`](../../../../../../test/fixtures/PeerTestHarness.ts)
(which is just `scenario.exec(fn.toString(), args).request(options)`).

- **No guards.** `ARpcService.guards` defaults to `[]`
  ([`ARpcService`](../../../../../../src/rpc/ARpcService.ts)) and **not one** harness
  service sets it. `runRPC` only runs guards when `this.guards.length`, so every
  harness endpoint is dispatched to any caller that reaches `onRpc`. The
  root's own header comment records this as intentional ("no guards — the
  harness must be able to query a peer before any handshake").
- **Reachable from the network, not just loopback.** Service resolution is
  structural — any root property exposing `runRPC` is dispatchable, from **any**
  transport ([rpc/README.md](../../../implementation/views/architecture/sdk/rpc/README.md) §6.4). The trusted-loopback
  bypass (INV-RPC-3) is what lets an _unguarded_ service skip guards, but an
  unguarded service needs no bypass at all. Any peer connected to a
  harness-built peer can therefore send `{service:"scenario", method:"exec",
params:[…]}` and obtain arbitrary code execution in that peer's host realm,
  or `{service:"handshake", method:"signMessage"}` to use it as a signing
  oracle. The same custom-root peer→peer dispatch path is demonstrated working
  in [test/e2e/E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts).
- **Not excluded from the published build.** `tsc` compiles the fixture tree
  into `dist/test/fixtures/customRpc/harnessControl/`, `package.json` `files`
  ships `dist`, and `exports["./test-harness"]` publishes
  [`test-harness.ts`](../../../../../../test-harness.ts), which re-exports
  `HarnessControlRpc` and `PeerTestHarness`. The root is therefore present in
  the npm package and is the **default** manifest for every peer any downstream
  consumer builds with `PeerTestHarness`.

**Per-test lifecycle.** `PeerTestHarness.setup(n)` resolves config once, boots or
adopts infrastructure, deploys contracts, then creates all peers **concurrently**
(`Promise.all` over `createPeer`, each a full `p2pSetup`) and indexes them by peer
index so ordering is stable regardless of completion order. `cleanup()` calls
`dispose()` on every peer concurrently (`Promise.allSettled`), then tears down
discovery, provider, and any node it started;
[`TestSession`](../../../../../../test/harness/session/TestSession.ts) owns the
per-test reset and the detached-error accounting. Peers are stopped by the
normal port-level `dispose` handshake — the harness has no separate kill path
(§3.5).

1. **Peers inside one test process.** All peers of a test share a process; each
   peer's host is inline or a `worker_thread` per `RUN_SDK_IN_THREAD`, and each
   host's EVM is inline or a second worker per `VM_DEDICATED_THREAD`
   ([`PeerTestHarness`](../../../../../../test/fixtures/PeerTestHarness.ts)).
2. **Test processes on one machine.** `yarn test:parallel`
   ([`scripts/test-e2e-parallel.js`](../../../../../../scripts/test-e2e-parallel.js))
   shards at **one Mocha `it()` per detached OS process** — `taskDiscovery`
   parses `it`/`describe` titles with ts-morph (falling back to loading the file
   when a title is not a literal) and each task runs
   `hardhat test --no-compile <file> --grep "^<title>$"`.
3. **Machines.** `--distributed` adds remote _test processes_, not remote peers:
   an orchestrator and one or more `test:parallel:server` hosts meet over a
   Hyperswarm DHT (topics and an HMAC challenge/response derived from
   `SCP_TEST_POOL_SECRET`,
   [`distributed/authentication.js`](../../../../../../scripts/e2e-parallel/distributed/authentication.js)),
   the orchestrator ships a source bundle, and workers **pull** task assignments
   ([`distributed/orchestrator.js`](../../../../../../scripts/e2e-parallel/distributed/orchestrator.js),
   [`server.js`](../../../../../../scripts/e2e-parallel/distributed/server.js)).

**Scheduling model.** Admission, not interleaving. The local scheduler
([`local/scheduler.js`](../../../../../../scripts/e2e-parallel/local/scheduler.js))
ticks once a second and admits a queued task only if a CPU/memory
gate allows it
([`shared/resourceGate.js`](../../../../../../scripts/e2e-parallel/shared/resourceGate.js):
CPU utilization against a target load, RSS of live children against a memory
bound, with a running per-test mean), always admitting when nothing is running so
progress is guaranteed. The queue is FIFO with monotonic attempt ids
([`shared/taskCoordinator.js`](../../../../../../scripts/e2e-parallel/shared/taskCoordinator.js));
the distributed coordinator additionally runs **speculative** duplicates of the
newest in-flight task for straggler mitigation and drops redundant results. Two
bounded retry classes exist — one event-loop-starvation retry and one
infrastructure retry. **No component orders events across peers**: within a test,
coordination is client-side polling
([`waitFor`](../../../../../../test/utils/waitFor.ts)), event barriers over mirrored
bus events, tip convergence checks
([`SyncCoordinator`](../../../../../../test/utils/SyncCoordinator.ts)), and
`quiesceHosts()` over the port. The only _deterministic_ interleaving control is
per-peer and cooperative: the `stub` service's hold/release/replay pairs
(reduction tasks, `SnapshotUpdated` / `DisputeCommitted` / `CalldataPosted`
events, paused reduction and paused `constructDispute`) let a test freeze one
peer at a named point and release it. **Open question:** there is no
cross-peer deterministic scheduler and no specified determinism guarantee; tests
are wall-clock/polling based and rely on generous timeouts. Whether the
specification should commit to a deterministic multi-peer interleaving mechanism
(virtual time, a step-scheduler over the ports) or accept the current
polling model is undecided. (Divergence class: decision pending.)

**Per-run isolation (REQ-RUN-11).** Isolation is by partitioning, not by
sandboxing. A "slot" is one hardhat node plus one local-discovery registry plus a
wiped manager-cache directory on OS-assigned free ports
([`test/utils/nodeInfra.js`](../../../../../../test/utils/nodeInfra.js)); the default is
**one slot shared by all concurrent test processes**, and concurrent processes
are kept apart by disjoint account ranges — `E2E_SLOT_INDEX × SLOT_STRIDE`
([`slotAccounts.ts`](../../../../../../test/harness/core/slotAccounts.ts), stride 10,
deployer at the top of each stride, against a 400-account pool) — plus a channel
id stamped with time, pid, and randomness
([`PeerTestHarness`](../../../../../../test/fixtures/PeerTestHarness.ts)). Remote
workers additionally get a per-lease temp workspace and an allowlisted
environment rather than an inherited one
([`distributed/remoteEnvironment.js`](../../../../../../scripts/e2e-parallel/distributed/remoteEnvironment.js)).
_Current:_ two concurrent tests share a chain and a discovery registry and are
isolated only by account range and channel id; a test that touches global chain
state (mining mode, block timestamps) is not isolated from its neighbours.
**Open question:** whether shared-slot execution is an accepted constraint or
whether the isolation guarantee should be "one chain per test process". Divergence
class: **decision pending**.

**What the code gives.**
[test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts)
runs the same test body once per combination of
`RUN_SDK_IN_THREAD × VM_DEDICATED_THREAD` (all four), building a real peer
against a hardhat node and asserting, in each mode: the chain and p2p signer
report the same address; `getAllTimes()` returns four entries; contract reads
return well-formed data; `resolveName` fails with `UNSUPPORTED_OPERATION`;
`call`/`estimateGas`/`populateCall`/`populateTransaction` produce the expected
values; message, byte-message, and typed-data signatures recover to the host
address; a `from`-mismatch send fails with `INVALID_ARGUMENT` and the expected
`shortMessage`; and two concurrent sends receive consecutive nonces with
successful receipts. Every peer is disposed in a `finally`.

- **Equivalence matrix (REQ-RUN-1, REQ-RUN-12 partial):**
  [test/e2e/E2E-RuntimeTransportModes.test.ts](../../../../../../test/e2e/E2E-RuntimeTransportModes.test.ts)
  — all four flag combinations, per §11.6.
- **Control-surface addressing (REQ-RUN-8):**
  [test/e2e/E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts)
  drives `hostRpc.network.connectToChannel` (a harness-control service) and both
  loopback and peer-addressed custom-RPC calls, including remote error
  propagation back across the port;
  [test/e2e/E2E-PingService.test.ts](../../../../../../test/e2e/E2E-PingService.test.ts)
  covers fire-and-forget peer targeting.
- **The surface in use (REQ-RUN-9, INV-RUN-5):** the `test/e2e` and `test/unit`
  suites are the standing evidence — every white-box assertion in them is a
  serialized harness-control call
  ([`test/harness/actions/`](../../../../../../test/harness/actions),
  [`SyncCoordinator`](../../../../../../test/utils/SyncCoordinator.ts)).
- **Runner machinery:**
  [test/scripts/](../../../../../../test/scripts) covers task discovery, the
  coordinator, the worker scheduler, leases, the distributed protocol and
  lifecycle, workspace caching/preparation, and log-directory handling.
- **Gaps.** No test asserts that a harness endpoint is (or is not) reachable
  from a network transport, so REQ-RUN-10 has no evidence in either direction.
  No test pins per-peer stub isolation (INV-RUN-6) — it is structural. No test
  compares any observation _across_ transport modes (REQ-RUN-12). No multi-peer
  protocol scenario runs in more than one mode. No test asserts the per-slot
  isolation properties of REQ-RUN-11 (account disjointness, channel-id
  uniqueness).

| Requirement / invariant | Statement                                                                                                                                                                                                                                                                                                                                   | Implementation status  | Implementation evidence                                                                                                                                                                                                                                                                                                                                                                                                         | Gap / divergence                                                           |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `REQ-RUN-1`             | Communication is serialized messages over paired ports; the protocol and observable behavior are identical inline or worker, so callers keep no separate implementations and a component can move to a worker without changing its contract.                                                                                                | Claimed; audit pending | [src/evm/p2pRuntime/types.ts](../../../../../../src/evm/p2pRuntime/types.ts), [node/P2pRuntimeChannel.ts](../../../../../../src/evm/p2pRuntime/node/P2pRuntimeChannel.ts), [P2pRuntimeClient.ts](../../../../../../src/evm/p2pRuntime/P2pRuntimeClient.ts), [P2pRuntimeHost.ts](../../../../../../src/evm/p2pRuntime/P2pRuntimeHost.ts), [EvmDiamondStateMachine.p2pSetup](../../../../../../src/evm/EvmDiamondStateMachine.ts) | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-2`             | No shared mutable memory between main thread, host, and executor; all inter-context interaction is a serialized message.                                                                                                                                                                                                                    | Claimed; audit pending | [src/evm/p2pRuntime](../../../../../../src/evm/p2pRuntime), [src/evm/contractExecutor](../../../../../../src/evm/contractExecutor)                                                                                                                                                                                                                                                                                              | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-3`             | Internal state is owned by the receiving component; the app cannot reach node state, the host cannot reach EVM state, except by a request.                                                                                                                                                                                                  | Claimed; audit pending | [src/evm/P2pInstance.ts](../../../../../../src/evm/P2pInstance.ts) (`getStateManager` throws), [P2pRuntimeHost.ts](../../../../../../src/evm/p2pRuntime/P2pRuntimeHost.ts), [contractExecutor/WorkerContractExecutor.ts](../../../../../../src/evm/contractExecutor/WorkerContractExecutor.ts)                                                                                                                                  | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-4`             | Ownership, ordering, lifecycle, error, disposal, and serialization rules for a cross-boundary message are explicit before a component moves to a worker.                                                                                                                                                                                    | Claimed; audit pending | this document §3; [P2pRuntimeHost.ts](../../../../../../src/evm/p2pRuntime/P2pRuntimeHost.ts), [P2pRuntimeClient.ts](../../../../../../src/evm/p2pRuntime/P2pRuntimeClient.ts), [chainSignerSerialization.ts](../../../../../../src/evm/p2pRuntime/chainSignerSerialization.ts)                                                                                                                                                 | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-5`             | Every client→host request carries a client-local `requestId`; a single handler per port correlates the one matching response; only the paired context can settle it.                                                                                                                                                                        | Claimed; audit pending | [P2pRuntimeClient.ts](../../../../../../src/evm/p2pRuntime/P2pRuntimeClient.ts) (`request`, `handleResponse`), [P2pRuntimeHost.ts](../../../../../../src/evm/p2pRuntime/P2pRuntimeHost.ts) (`handleRequest`), [worker/protocol.ts](../../../../../../src/evm/p2pRuntime/worker/protocol.ts)                                                                                                                                     | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-6`             | Cross-boundary values obey structured-clone limits: bigints as quantity/Codec strings, manifests not class instances, only ports transferred; `customData`/`kzg` rejected; non-serializable bus args drop.                                                                                                                                  | Claimed; audit pending | [chainSignerSerialization.ts](../../../../../../src/evm/p2pRuntime/chainSignerSerialization.ts), [worker/protocol.ts](../../../../../../src/evm/p2pRuntime/worker/protocol.ts), [rpc/registry.ts](../../../../../../src/rpc/registry.ts)                                                                                                                                                                                        | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-7`             | Worker isolation is a client performance strategy (not server scaling), applied incrementally where measurement justifies it; each boundary should be measurement-justified.                                                                                                                                                                | Claimed; audit pending | [src/utils/config.ts](../../../../../../src/utils/config.ts) (`RUN_SDK_IN_THREAD`, `VM_DEDICATED_THREAD` default false), [node/workerResourceLimits.ts](../../../../../../src/evm/node/workerResourceLimits.ts), [node/workerStartupTiming.ts](../../../../../../src/evm/node/workerStartupTiming.ts)                                                                                                                           | Engineer audit pending; any divergence named in the evidence remains open. |
| `INV-RUN-1`             | Ports parallelize across contexts; the host mutex serializes live-state mutation, preserved across the executor worker because executor calls are awaited under the mutex.                                                                                                                                                                  | Claimed; audit pending | [src/stateManager/StateManager.ts](../../../../../../src/stateManager/StateManager.ts), [contractExecutor/WorkerContractExecutor.ts](../../../../../../src/evm/contractExecutor/WorkerContractExecutor.ts); relation owned by [block-confirmation-pipeline.md](../../../implementation/views/architecture/sdk/block-confirmation-pipeline.md) §3.1                                                                              | Engineer audit pending; any divergence named in the evidence remains open. |
| `INV-RUN-2`             | 1:1 port pair; one transfer owner; one handler per port; correlation trusts the paired writer.                                                                                                                                                                                                                                              | Claimed; audit pending | [node/P2pRuntimeChannel.ts](../../../../../../src/evm/p2pRuntime/node/P2pRuntimeChannel.ts), [P2pRuntimeClient.ts](../../../../../../src/evm/p2pRuntime/P2pRuntimeClient.ts), [P2pRuntimeHost.ts](../../../../../../src/evm/p2pRuntime/P2pRuntimeHost.ts)                                                                                                                                                                       | Engineer audit pending; any divergence named in the evidence remains open. |
| `INV-RUN-3`             | Host construction failure posts `hostError` + closes the port (client `ready` rejects, pending reject); dead client port self-disposes the host.                                                                                                                                                                                            | Claimed; audit pending | [P2pRuntimeHost.ts](../../../../../../src/evm/p2pRuntime/P2pRuntimeHost.ts) (early `catch`, `port.onClose`), [P2pRuntimeClient.ts](../../../../../../src/evm/p2pRuntime/P2pRuntimeClient.ts) (`dispatchHostError`, `handlePortClosed`)                                                                                                                                                                                          | Engineer audit pending; any divergence named in the evidence remains open. |
| `INV-RUN-4`             | The signing key and nonce never cross a boundary; only the host signs; the client holds proxy signers.                                                                                                                                                                                                                                      | Claimed; audit pending | [P2pRuntimeHost.ts](../../../../../../src/evm/p2pRuntime/P2pRuntimeHost.ts), [RuntimeChainContext.ts](../../../../../../src/evm/p2pRuntime/RuntimeChainContext.ts), [signer/HostNonceManager.ts](../../../../../../src/evm/signer/HostNonceManager.ts)                                                                                                                                                                          | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-8`             | Harness control is a custom-RPC root over the same serialized surfaces; a controller drives a peer only through `hostRpc` (loopback to that peer's host, or a typed relay to a peer address), never by in-process reference.                                                                                                                | Claimed; audit pending | [test/fixtures/customRpc/harnessControl/HarnessControlRpc.ts](../../../../../../test/fixtures/customRpc/harnessControl/HarnessControlRpc.ts), [test/fixtures/PeerTestHarness.ts](../../../../../../test/fixtures/PeerTestHarness.ts) (`control`, `resolveHarnessRpcManifest`), [ClientHostRpc.ts](../../../../../../src/evm/p2pRuntime/ClientHostRpc.ts)                                                                        | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-9`             | Every harness-control operation returns structured-clone-serializable projections; bigint-bearing structs cross `Codec.encode`d and are decoded in the host endpoint.                                                                                                                                                                       | Claimed; audit pending | [harnessControl/services](../../../../../../test/fixtures/customRpc/harnessControl/services) (`encoded*` endpoints), [src/utils/Codec.ts](../../../../../../src/utils/Codec.ts)                                                                                                                                                                                                                                                 | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-10`            | The harness-control root is not reachable by a network peer and is not present in a production artifact.                                                                                                                                                                                                                                    | Partial or divergent   | _Current:_ no guard on any harness service ([src/rpc/ARpcService.ts](../../../../../../src/rpc/ARpcService.ts) `guards = []`), root compiled into `dist/test/...` and re-exported by [test-harness.ts](../../../../../../test-harness.ts) via `exports["./test-harness"]` ([package.json](../../../../../../package.json)). _Intended:_ trusted-transport guard + build exclusion                                               | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-11`            | Concurrently executing peers and test processes are isolated: per-peer host realm and service state, disjoint account partitions per process, per-test channel id, per-slot chain/discovery/cache.                                                                                                                                          | Claimed; audit pending | [test/harness/core/slotAccounts.ts](../../../../../../test/harness/core/slotAccounts.ts), [test/utils/nodeInfra.js](../../../../../../test/utils/nodeInfra.js), [scripts/e2e-parallel/shared/scheduling.js](../../../../../../scripts/e2e-parallel/shared/scheduling.js), [test/fixtures/PeerTestHarness.ts](../../../../../../test/fixtures/PeerTestHarness.ts)                                                                | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-12`            | The inline-vs-worker equivalence criterion states which observations must be identical across `RUN_SDK_IN_THREAD × VM_DEDICATED_THREAD`, and the matrix test is that criterion's oracle.                                                                                                                                                    | Claimed; audit pending | criterion now stated (REQ-RUN-15); oracle not built                                                                                                                                                                                                                                                                                                                                                                             | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-13`            | Both worker boundaries (runtime host, EVM executor) are the intended defaults, uniformly — no per-environment profile branching; today's off-by-default state is a maturity gap. Engineer decision 2026-08-10 (§6.1).                                                                                                                       | Partial or divergent   | _Current:_ `RUN_SDK_IN_THREAD` / `VM_DEDICATED_THREAD` default `false` ([config.ts](../../../../../../src/utils/config.ts)); parallel gate runs both on ([scripts/e2e-parallel/local/scheduler.js](../../../../../../scripts/e2e-parallel/local/scheduler.js))                                                                                                                                                                  | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-14`            | A ~6-participant channel must run within a mid-range mobile browser envelope (~4 GB device RAM, few-hundred-MB usable heap, mobile CPU). The envelope is a hard implementation budget: three contexts must be made to fit it. The 1024 MB worker cap is CI containment and is too high for this floor. Engineer decision 2026-08-10 (§6.1). | Missing                | none — gap (no budget derived from this envelope; [workerResourceLimits.ts](../../../../../../src/evm/node/workerResourceLimits.ts) is CI-scoped)                                                                                                                                                                                                                                                                               | Engineer audit pending; any divergence named in the evidence remains open. |
| `REQ-RUN-15`            | Inline and worker mode are equivalent when observable protocol behavior matches: blocks/signatures/state roots, on-chain actions, emitted events and payloads, and error identity. Timing and internal interleaving are out of scope.                                                                                                       | Missing                | none — gap (oracle not built)                                                                                                                                                                                                                                                                                                                                                                                                   | Engineer audit pending; any divergence named in the evidence remains open. |
| `INV-RUN-5`             | A harness-control operation executes in the host realm on live objects and returns only serializable projections; no live `Block`, transport, profile, or `StateManager` reference crosses the port.                                                                                                                                        | Claimed; audit pending | [harnessControl/services](../../../../../../test/fixtures/customRpc/harnessControl/services), [src/evm/P2pInstance.ts](../../../../../../src/evm/P2pInstance.ts) (`getStateManager` throws)                                                                                                                                                                                                                                     | Engineer audit pending; any divergence named in the evidence remains open. |
| `INV-RUN-6`             | Harness stub state (installed doubles, saved originals, registered peer keys) is scoped to one peer's host service instance, so co-located inline hosts do not leak faults between peers.                                                                                                                                                   | Claimed; audit pending | [harnessControl/services/stub/StubService.ts](../../../../../../test/fixtures/customRpc/harnessControl/services/stub/StubService.ts), [StubRpcMethods.ts](../../../../../../test/fixtures/customRpc/harnessControl/services/stub/StubRpcMethods.ts) (instance-level assignment only; no prototype patching in the fixture tree)                                                                                                 | Engineer audit pending; any divergence named in the evidence remains open. |

## operations/configuration.md

## Test traceability

| Permutation       | Behavior                                 | Implementation obligations                                                                                                       | Test status                        | Exact test evidence                                                     | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `REQ-CFG-1.T1.P1` | default only                             | Verify `REQ-CFG-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-1.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-1.T1.P2` | file over default                        | Verify `REQ-CFG-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-1.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-1.T1.P3` | environment over file                    | Verify `REQ-CFG-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-1.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-1.T1.P4` | explicit override over environment       | Verify `REQ-CFG-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-1.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-1.T1.P5` | unrelated keys unchanged                 | Verify `REQ-CFG-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-1.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-2.T1.P1` | every accepted boolean spelling and case | Verify `REQ-CFG-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-2.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-2.T1.P2` | finite/non-finite number                 | Verify `REQ-CFG-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-2.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-2.T1.P3` | JSON and delimited arrays                | Verify `REQ-CFG-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-2.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-2.T1.P4` | malformed JSON                           | Verify `REQ-CFG-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-2.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-2.T1.P5` | empty and unknown values                 | Verify `REQ-CFG-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-2.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-3.T1.P1` | Node                                     | Verify `REQ-CFG-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-3.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-3.T1.P2` | browser                                  | Verify `REQ-CFG-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-3.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-3.T1.P3` | worker-host startup                      | Verify `REQ-CFG-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-3.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-3.T1.P4` | explicit override in both runtimes       | Verify `REQ-CFG-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-3.T1`. | Partial; permutation audit pending | `src/utils/config.ts`; exact mapped test: none — gap                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-4.T1.P1` | main-thread runtime                      | Verify `REQ-CFG-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-4.T1`. | Partial; permutation audit pending | `src/utils/config.ts`, `src/p2pSetup.ts`; exact mapped test: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-4.T1.P2` | dedicated runtime thread                 | Verify `REQ-CFG-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-4.T1`. | Partial; permutation audit pending | `src/utils/config.ts`, `src/p2pSetup.ts`; exact mapped test: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-4.T1.P3` | repeated component reads                 | Verify `REQ-CFG-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-4.T1`. | Partial; permutation audit pending | `src/utils/config.ts`, `src/p2pSetup.ts`; exact mapped test: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-4.T1.P4` | fresh runtime initialization             | Verify `REQ-CFG-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-4.T1`. | Partial; permutation audit pending | `src/utils/config.ts`, `src/p2pSetup.ts`; exact mapped test: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-5.T1.P1` | crash-log token                          | Verify `REQ-CFG-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-5.T1`. | Partial; permutation audit pending | `src/utils/config.ts`, `peer3.config.ts`; exact mapped test: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-5.T1.P2` | credential-bearing provider URL          | Verify `REQ-CFG-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-5.T1`. | Partial; permutation audit pending | `src/utils/config.ts`, `peer3.config.ts`; exact mapped test: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-5.T1.P3` | absent secret                            | Verify `REQ-CFG-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-5.T1`. | Partial; permutation audit pending | `src/utils/config.ts`, `peer3.config.ts`; exact mapped test: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-CFG-5.T1.P4` | explicit override                        | Verify `REQ-CFG-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-CFG-5.T1`. | Partial; permutation audit pending | `src/utils/config.ts`, `peer3.config.ts`; exact mapped test: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

| Command                                                  | Does                                                                                                                                                                                                                                                                           |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `yarn && yarn build`                                     | Install dependencies, then compile contracts + TypeChain and build the SDK and browser bundle (`compile` → `tsc` + `tsc-alias` → test-runtime utils → browser build → `npm pack`). Required before running examples or tests — they use this local build, not the npm package. |
| `yarn compile`                                           | Clean and compile contracts, generate TypeChain types, enums, and artifacts.                                                                                                                                                                                                   |
| `yarn test:parallel`                                     | **The canonical full test gate.** Runs all Mocha tests across parallel local worker processes; pass `--e2e-only` to limit discovery to `test/e2e`. Each run writes to a fresh `./logs/run-N/`.                                                                                 |
| `yarn test:parallel:distributed`                         | The same suite across distributed workers coordinated by a test server.                                                                                                                                                                                                        |
| `yarn test:parallel:server`                              | Start the distributed coordination server.                                                                                                                                                                                                                                     |
| `yarn test:parallel:prepare`                             | Compile and build everything a parallel/distributed run needs.                                                                                                                                                                                                                 |
| `yarn test`                                              | Legacy in-process run of the non-e2e tests (`--no-compile`, excludes `test/e2e`). Only for rare focused compatibility checks.                                                                                                                                                  |
| `yarn test:compile`                                      | `yarn compile` then `yarn test`.                                                                                                                                                                                                                                               |
| `yarn test:e2e`                                          | In-process end-to-end suite (`E2E_INTERVAL_MINING=1`, `LOG_LEVEL=warn`).                                                                                                                                                                                                       |
| `yarn test:unit`                                         | In-process run scoped to `test/unit` via `TEST_DIR`.                                                                                                                                                                                                                           |
| `yarn infra:hardhat-node` / `yarn infra:local-discovery` | Start the local chain / the local peer-discovery registry used by parallel runs.                                                                                                                                                                                               |
| `yarn format` / `yarn format:check`                      | Apply / verify Prettier formatting (also enforced by a Husky pre-commit hook).                                                                                                                                                                                                 |
| `yarn lint:fix`                                          | ESLint with autofix over `src` and `test`.                                                                                                                                                                                                                                     |

## architecture/sdk/rpc/README.md

## Test traceability

| Permutation       | Behavior                                | Implementation obligations                                                                                                       | Test status                        | Exact test evidence                                                                                                                                                     | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ----------------- | --------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `REQ-RPC-1.T1.P1` | Own/prototype/private/helper names      | Verify `REQ-RPC-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-1.T1`. | Missing                            | none — gap (structural convention; no lint/test enforces it)                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-1.T1.P2` | missing/existing service and method     | Verify `REQ-RPC-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-1.T1`. | Missing                            | none — gap (structural convention; no lint/test enforces it)                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-1.T1.P3` | crafted method names                    | Verify `REQ-RPC-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-1.T1`. | Missing                            | none — gap (structural convention; no lint/test enforces it)                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-1.T1.P4` | every service class.                    | Verify `REQ-RPC-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-1.T1`. | Missing                            | none — gap (structural convention; no lint/test enforces it)                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-2.T1.P1` | Valid value                             | Verify `REQ-RPC-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-2.T1`. | Partial; permutation audit pending | [test/rpc](../../../../../../test/rpc), [test/unit/SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts); adversarial fuzz gap.                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-2.T1.P2` | wrong type/tag/domain                   | Verify `REQ-RPC-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-2.T1`. | Partial; permutation audit pending | [test/rpc](../../../../../../test/rpc), [test/unit/SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts); adversarial fuzz gap.                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-2.T1.P3` | truncated/trailing data                 | Verify `REQ-RPC-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-2.T1`. | Partial; permutation audit pending | [test/rpc](../../../../../../test/rpc), [test/unit/SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts); adversarial fuzz gap.                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-2.T1.P4` | boundary values                         | Verify `REQ-RPC-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-2.T1`. | Partial; permutation audit pending | [test/rpc](../../../../../../test/rpc), [test/unit/SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts); adversarial fuzz gap.                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-2.T1.P5` | adversarial fuzz                        | Verify `REQ-RPC-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-2.T1`. | Partial; permutation audit pending | [test/rpc](../../../../../../test/rpc), [test/unit/SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts); adversarial fuzz gap.                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-2.T1.P6` | handler side-effect sentinel.           | Verify `REQ-RPC-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-2.T1`. | Partial; permutation audit pending | [test/rpc](../../../../../../test/rpc), [test/unit/SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts); adversarial fuzz gap.                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-3.T1.P1` | Absent/present/duplicate root           | Verify `REQ-RPC-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-3.T1`. | Partial; permutation audit pending | [E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts)                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-3.T1.P2` | malformed manifest                      | Verify `REQ-RPC-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-3.T1`. | Partial; permutation audit pending | [E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts)                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-3.T1.P3` | inline and worker runtime               | Verify `REQ-RPC-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-3.T1`. | Partial; permutation audit pending | [E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts)                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-3.T1.P4` | production and test-harness roots.      | Verify `REQ-RPC-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-3.T1`. | Partial; permutation audit pending | [E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts)                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-4.T1.P1` | Zero/max/safe-limit+1 bigint            | Verify `REQ-RPC-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-4.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-4.T1.P2` | nested structs and arrays               | Verify `REQ-RPC-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-4.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-4.T1.P3` | Codec string                            | Verify `REQ-RPC-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-4.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-4.T1.P4` | raw bigint                              | Verify `REQ-RPC-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-4.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-4.T1.P5` | worker and network port.                | Verify `REQ-RPC-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-4.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-4.T1.P1` | Request/response/error envelopes        | Verify `INV-RPC-4` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-4.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-4.T1.P2` | nested arrays/objects                   | Verify `INV-RPC-4` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-4.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-4.T1.P3` | `Uint8Array`                            | Verify `INV-RPC-4` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-4.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-4.T1.P4` | every encoded struct endpoint.          | Verify `INV-RPC-4` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-4.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-5.T1.P1` | Each guard pass/fail                    | Verify `REQ-RPC-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-5.T1`. | Partial; permutation audit pending | [E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts); per-service bypass matrix gap.                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-5.T1.P2` | ordering interactions                   | Verify `REQ-RPC-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-5.T1`. | Partial; permutation audit pending | [E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts); per-service bypass matrix gap.                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-5.T1.P3` | pre/post-handshake                      | Verify `REQ-RPC-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-5.T1`. | Partial; permutation audit pending | [E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts); per-service bypass matrix gap.                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-5.T1.P4` | loopback versus every network transport | Verify `REQ-RPC-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-5.T1`. | Partial; permutation audit pending | [E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts); per-service bypass matrix gap.                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-5.T1.P5` | retry queue.                            | Verify `REQ-RPC-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-5.T1`. | Partial; permutation audit pending | [E2E-InitHandshake.test.ts](../../../../../../test/e2e/E2E-InitHandshake.test.ts); per-service bypass matrix gap.                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-6.T1.P1` | Duplicate in-flight/completed request   | Verify `REQ-RPC-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-6.T1`. | Partial; permutation audit pending | [E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts); systematic replay matrix gap.                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-6.T1.P2` | late response                           | Verify `REQ-RPC-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-6.T1`. | Partial; permutation audit pending | [E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts); systematic replay matrix gap.                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-6.T1.P3` | retry after transport loss              | Verify `REQ-RPC-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-6.T1`. | Partial; permutation audit pending | [E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts); systematic replay matrix gap.                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-6.T1.P4` | reordered concurrent requests           | Verify `REQ-RPC-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-6.T1`. | Partial; permutation audit pending | [E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts); systematic replay matrix gap.                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-6.T1.P5` | every mutating endpoint.                | Verify `REQ-RPC-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-6.T1`. | Partial; permutation audit pending | [E2E-IsForkDisputed.test.ts](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts); systematic replay matrix gap.                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-1.T1.P1` | Failure and boundary at every stage     | Verify `INV-RPC-1` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-1.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-1.T1.P2` | malformed and oversized frame           | Verify `INV-RPC-1` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-1.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-1.T1.P3` | absent service/method                   | Verify `INV-RPC-1` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-1.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-1.T1.P4` | guard rejection/bypass.                 | Verify `INV-RPC-1` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-1.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts)                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-2.T1.P1` | Correct/wrong peer                      | Verify `INV-RPC-2` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-2.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts), [E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-2.T1.P2` | transport replacement for same identity | Verify `INV-RPC-2` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-2.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts), [E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-2.T1.P3` | concurrent/late/duplicate response      | Verify `INV-RPC-2` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-2.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts), [E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-2.T1.P4` | requestId collision.                    | Verify `INV-RPC-2` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-2.T1`. | Partial; permutation audit pending | [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts), [E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-3.T1.P1` | Loopback and every network transport    | Verify `INV-RPC-3` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-3.T1`. | Missing                            | none — gap                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-3.T1.P2` | forged trust metadata                   | Verify `INV-RPC-3` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-3.T1`. | Missing                            | none — gap                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-3.T1.P3` | transport upgrade/replacement           | Verify `INV-RPC-3` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-3.T1`. | Missing                            | none — gap                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-3.T1.P4` | pre-handshake request.                  | Verify `INV-RPC-3` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-3.T1`. | Missing                            | none — gap                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-5.T1.P1` | Read-only/mutating endpoints            | Verify `INV-RPC-5` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-5.T1`. | Partial; permutation audit pending | [block-confirmation-pipeline.md](../../../implementation/views/architecture/sdk/block-confirmation-pipeline.md) §12; dedicated ownership evidence gap.                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-5.T1.P2` | concurrent intake                       | Verify `INV-RPC-5` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-5.T1`. | Partial; permutation audit pending | [block-confirmation-pipeline.md](../../../implementation/views/architecture/sdk/block-confirmation-pipeline.md) §12; dedicated ownership evidence gap.                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-5.T1.P3` | handler error                           | Verify `INV-RPC-5` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-5.T1`. | Partial; permutation audit pending | [block-confirmation-pipeline.md](../../../implementation/views/architecture/sdk/block-confirmation-pipeline.md) §12; dedicated ownership evidence gap.                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-5.T1.P4` | retry                                   | Verify `INV-RPC-5` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-5.T1`. | Partial; permutation audit pending | [block-confirmation-pipeline.md](../../../implementation/views/architecture/sdk/block-confirmation-pipeline.md) §12; dedicated ownership evidence gap.                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-RPC-5.T1.P5` | inline and worker runtime.              | Verify `INV-RPC-5` through the matching implementation conformance row and the concrete obligations refined from `INV-RPC-5.T1`. | Partial; permutation audit pending | [block-confirmation-pipeline.md](../../../implementation/views/architecture/sdk/block-confirmation-pipeline.md) §12; dedicated ownership evidence gap.                  | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-7.T1.P1` | Per-peer/global saturation              | Verify `REQ-RPC-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-7.T1`. | Missing                            | none — not implemented ([OQ-6](../../../specification/open-questions.md))                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-7.T1.P2` | cheap/expensive endpoints               | Verify `REQ-RPC-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-7.T1`. | Missing                            | none — not implemented ([OQ-6](../../../specification/open-questions.md))                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-7.T1.P3` | reconnect and identity rotation         | Verify `REQ-RPC-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-7.T1`. | Missing                            | none — not implemented ([OQ-6](../../../specification/open-questions.md))                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-7.T1.P4` | burst/sustained load                    | Verify `REQ-RPC-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-7.T1`. | Missing                            | none — not implemented ([OQ-6](../../../specification/open-questions.md))                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-RPC-7.T1.P5` | recovery after refill.                  | Verify `REQ-RPC-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-RPC-7.T1`. | Missing                            | none — not implemented ([OQ-6](../../../specification/open-questions.md))                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

A manifest (module specifier) rather than a class instance is used because the RPC root must be
constructed inside the runtime host realm — possibly a worker thread — where a main-thread class
instance cannot be transferred. Verification:
[test/e2e/E2E-CustomRpcRequestResponse.test.ts](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts).

- **Unit / boundary:** [test/rpc/Rpc.test.ts](../../../../../../test/rpc/Rpc.test.ts) (envelope,
  serialization, correlation); [test/rpc/initHandshake](../../../../../../test/rpc/initHandshake),
  [test/rpc/joinChannel](../../../../../../test/rpc/joinChannel),
  [test/rpc/openChannelNegotiation](../../../../../../test/rpc/openChannelNegotiation);
  [test/unit/SpectateService.test.ts](../../../../../../test/unit/SpectateService.test.ts).
- **End-to-end:** [E2E-InitHandshake](../../../../../../test/e2e/E2E-InitHandshake.test.ts)
  (authentication, signing-oracle hygiene),
  [E2E-CustomRpcRequestResponse](../../../../../../test/e2e/E2E-CustomRpcRequestResponse.test.ts)
  (custom root + request/response),
  [E2E-IsForkDisputed](../../../../../../test/e2e/E2E-IsForkDisputed.test.ts),
  [E2E-Spectate](../../../../../../test/e2e/E2E-Spectate.test.ts) /
  [E2E-SpectateStaleProofGuard](../../../../../../test/e2e/E2E-SpectateStaleProofGuard.test.ts),
  [E2E-ByzantineErrorAttribution](../../../../../../test/e2e/E2E-ByzantineErrorAttribution.test.ts).
- **What matters most (risk-first):** adversarial envelope fuzzing at `onRpc` (malformed frames,
  wrong-type `params`, prototype-name methods per the §2.1 open question); guard bypass attempts
  pre-handshake against every guarded service; correlation attacks (wrong-peer responses,
  requestId reuse, late responses); Codec round-trip and decode-failure behavior at every
  `encoded*` endpoint; the outcome table of §8 as executable assertions.
- **Known gaps:** no adversarial fuzz suite for the envelope; no test pinning the guard
  retry-queue request/response interaction (§5.2); no test that prototype-inherited methods are
  (or are not) dispatchable; no rate-limit tests (nothing to test yet — OQ-6); `none — gap`
  entries below.
