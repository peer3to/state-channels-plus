# Disputes — Verification

> **Agent authoring status:** Current verification evidence assembled; completeness and classifications require engineer verification.
> **Engineer verification:** Pending.

## Contents

- [Verification overview](#verification-overview)
    - [Specification-test adherence](#specification-test-adherence)
    - [Implementation-test adherence](#implementation-test-adherence)
    - [Contradictions](#contradictions)
    - [Missing](#missing)
- [Specification test traceability](#specification-test-traceability)
- [Implementation test traceability](#implementation-test-traceability)

## Verification overview

**Status:** Incomplete; candidate tests are known, but declaration-level setup and oracle review is
not complete for every specification and implementation permutation.

### Specification-test adherence

The existing candidate evidence is summarized below. These links are leads, not automatic credit; the traceability rows remain authoritative.

- **Facet unit/property tests:**
  `test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol`
  — reduce capacity/dedup fuzzing, slash-suppresses-timeout, timeout/self-removal removal
  combinations, kill-period expiry semantics (`applyDisputeFraudProofs`/`killDispute` reverts,
  atomic mixed batches), upload race checks, timeout-calldata proof validation including the
  first-block grace edge, inbound-chain verification failure reporting.
- **Dispute validation e2e:**
  `test/e2e/disputeValidation/` — per-input-field
  soundness (`disputeInputFields/`: forkId, timeout, selfRemoval, onChainSlashes, snapshot and
  auditing hashes), upload reverts including the throttle (`uploadRevert/`), state-proof case
  matrix (`stateProof/`), output-state, balance-invariant, not-latest-state, inbound-hash, and
  reduced-fork timestamp checks.
- **System e2e:** `test/e2e/` — `E2E-DisputeManager`, `E2E-FinalDispute`,
  `E2E-Timeouts`, `E2E-FirstBlockTimestampGrace`, `E2E-ForceJoinDispute` (forced inbound
  inclusion), `E2E-StaleMembershipDispute`, `E2E-IsForkDisputed`, `E2E-ReductionManager`,
  `E2E-StateSnapshots` (fork advancement), `E2E-Fuzz-Dispute-MVP` with the soundness menu in
  `test/e2e/fuzz/`.
- **Unit (SDK):** `test/unit/DisputeManager.test.ts`,
  `test/unit/ReductionExecutor.test.ts`.
- **Gaps (deliberate, must be closed before approval):** order-independence permutation tests and
  a convergence argument for `INV-DIS-5`; colluding-timeout and delayed-evidence-disclosure
  scenarios for §6.2; the all-killed-window path; multi-generation fork walks in
  `updateStateSnapshotFork` under adversarial interleaving.

Only rows with an exact declaration link and a specific setup/oracle assessment receive credit.
Rows that still cite a whole file, a neighboring behavior, or “none — gap” remain partial or missing.

### Implementation-test adherence

The matching implementation document defines the source-owned unit permutations and internal system
integration permutations. Each is listed in the bottom matrix. Until a real test declaration is inspected
and its setup, stimulus, oracle, failure behavior, and runtime are recorded, that permutation remains missing.

### Contradictions

No test has yet been confirmed to assert behavior opposite to this subject's specification or implementation
plan. This is not a clean bill of health: broad legacy links have been downgraded to partial or missing where
they do not prove the named permutation.

### Missing

- Replace every whole-file or descriptive evidence link with the exact declaration that performs the setup,
  stimulus, and assertion.
- Classify every applicable component, contract, integration, end-to-end, browser/node, worker, and
  distributed runtime boundary.
- Add or identify tests for every row marked missing; split tests whose oracle cannot prove each credited
  permutation independently.
- Recheck misleading or merely adjacent tests instead of using their filenames as evidence.

## Specification test traceability

This table judges every neutral specification permutation against the real test body. A file-level or adjacent test is not complete evidence.

| Permutation        | Behavior                                                                             | Implementation obligations                                                                                                         | Test status                        | Exact test evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ------------------ | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `REQ-DIS-1.T1.P1`  | valid case and direct invalid/opposite                                               | Verify `REQ-DIS-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-1.T1`.   | Partial; permutation audit pending | [`test/e2e/disputeValidation/disputeInputFields/`](../../../../../test/e2e/disputeValidation/disputeInputFields)                                                                                                                                                                                                                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-1.T1.P2`  | before/at/after deadline and maximum honest skew                                     | Verify `REQ-DIS-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-1.T1`.   | Partial; permutation audit pending | [`test/e2e/disputeValidation/disputeInputFields/`](../../../../../test/e2e/disputeValidation/disputeInputFields)                                                                                                                                                                                                                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-1.T1.P3`  | new/existing/removed/slashed participant and concurrent membership change            | Verify `REQ-DIS-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-1.T1`.   | Partial; permutation audit pending | [`test/e2e/disputeValidation/disputeInputFields/`](../../../../../test/e2e/disputeValidation/disputeInputFields)                                                                                                                                                                                                                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-1.T1.P4`  | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-DIS-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-1.T1`.   | Partial; permutation audit pending | [`test/e2e/disputeValidation/disputeInputFields/`](../../../../../test/e2e/disputeValidation/disputeInputFields)                                                                                                                                                                                                                                                                                                                                                              | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-2.T1.P1`  | valid case and direct invalid/opposite                                               | Verify `REQ-DIS-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-2.T1`.   | Partial; permutation audit pending | [`test/e2e/disputeValidation/uploadRevert/`](../../../../../test/e2e/disputeValidation/uploadRevert) (incl. `disputerThrottle.test.ts`, `disputer.test.ts`)                                                                                                                                                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-2.T1.P2`  | correct/wrong/missing/duplicate/forged identity or signature and membership boundary | Verify `REQ-DIS-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-2.T1`.   | Partial; permutation audit pending | [`test/e2e/disputeValidation/uploadRevert/`](../../../../../test/e2e/disputeValidation/uploadRevert) (incl. `disputerThrottle.test.ts`, `disputer.test.ts`)                                                                                                                                                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-2.T1.P3`  | before/at/after deadline and maximum honest skew                                     | Verify `REQ-DIS-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-2.T1`.   | Partial; permutation audit pending | [`test/e2e/disputeValidation/uploadRevert/`](../../../../../test/e2e/disputeValidation/uploadRevert) (incl. `disputerThrottle.test.ts`, `disputer.test.ts`)                                                                                                                                                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-2.T1.P4`  | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-DIS-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-2.T1`.   | Partial; permutation audit pending | [`test/e2e/disputeValidation/uploadRevert/`](../../../../../test/e2e/disputeValidation/uploadRevert) (incl. `disputerThrottle.test.ts`, `disputer.test.ts`)                                                                                                                                                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-3.T1.P1`  | valid case and direct invalid/opposite                                               | Verify `REQ-DIS-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-3.T1`.   | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (kill-period tests), [`test/e2e/disputeValidation/`](../../../../../test/e2e/disputeValidation)                                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-3.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `REQ-DIS-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-3.T1`.   | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (kill-period tests), [`test/e2e/disputeValidation/`](../../../../../test/e2e/disputeValidation)                                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-3.T1.P3`  | before/at/after deadline and maximum honest skew                                     | Verify `REQ-DIS-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-3.T1`.   | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (kill-period tests), [`test/e2e/disputeValidation/`](../../../../../test/e2e/disputeValidation)                                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-3.T1.P4`  | new/existing/removed/slashed participant and concurrent membership change            | Verify `REQ-DIS-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-3.T1`.   | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (kill-period tests), [`test/e2e/disputeValidation/`](../../../../../test/e2e/disputeValidation)                                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-3.T1.P5`  | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-DIS-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-3.T1`.   | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (kill-period tests), [`test/e2e/disputeValidation/`](../../../../../test/e2e/disputeValidation)                                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-4.T1.P1`  | valid case and direct invalid/opposite                                               | Verify `REQ-DIS-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-4.T1`.   | Partial; permutation audit pending | [`E2E-ReductionManager.test.ts`](../../../../../test/e2e/E2E-ReductionManager.test.ts), [`test/unit/ReductionExecutor.test.ts`](../../../../../test/unit/ReductionExecutor.test.ts)                                                                                                                                                                                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-4.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `REQ-DIS-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-4.T1`.   | Partial; permutation audit pending | [`E2E-ReductionManager.test.ts`](../../../../../test/e2e/E2E-ReductionManager.test.ts), [`test/unit/ReductionExecutor.test.ts`](../../../../../test/unit/ReductionExecutor.test.ts)                                                                                                                                                                                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-4.T1.P3`  | before/at/after deadline and maximum honest skew                                     | Verify `REQ-DIS-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-4.T1`.   | Partial; permutation audit pending | [`E2E-ReductionManager.test.ts`](../../../../../test/e2e/E2E-ReductionManager.test.ts), [`test/unit/ReductionExecutor.test.ts`](../../../../../test/unit/ReductionExecutor.test.ts)                                                                                                                                                                                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-4.T1.P4`  | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-DIS-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-4.T1`.   | Partial; permutation audit pending | [`E2E-ReductionManager.test.ts`](../../../../../test/e2e/E2E-ReductionManager.test.ts), [`test/unit/ReductionExecutor.test.ts`](../../../../../test/unit/ReductionExecutor.test.ts)                                                                                                                                                                                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-5.T1.P1`  | valid case and direct invalid/opposite                                               | Verify `INV-DIS-5` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-5.T1`.   | Missing                            | none — gap (permutation/adversarial-interleaving tests incl. kills, and convergence proof required)                                                                                                                                                                                                                                                                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-5.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `INV-DIS-5` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-5.T1`.   | Missing                            | none — gap (permutation/adversarial-interleaving tests incl. kills, and convergence proof required)                                                                                                                                                                                                                                                                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-5.T1.P3`  | new/existing/removed/slashed participant and concurrent membership change            | Verify `INV-DIS-5` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-5.T1`.   | Missing                            | none — gap (permutation/adversarial-interleaving tests incl. kills, and convergence proof required)                                                                                                                                                                                                                                                                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-5.T1.P4`  | duplicate/replay, every relevant permutation and concurrent delivery                 | Verify `INV-DIS-5` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-5.T1`.   | Missing                            | none — gap (permutation/adversarial-interleaving tests incl. kills, and convergence proof required)                                                                                                                                                                                                                                                                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-5.T1.P5`  | malformed and adversarial input, partial failure, retry and recovery                 | Verify `INV-DIS-5` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-5.T1`.   | Missing                            | none — gap (permutation/adversarial-interleaving tests incl. kills, and convergence proof required)                                                                                                                                                                                                                                                                                                                                                                           | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-6.T1.P1`  | valid case and direct invalid/opposite                                               | Verify `REQ-DIS-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-6.T1`.   | Partial; permutation audit pending | [`E2E-FinalDispute.test.ts`](../../../../../test/e2e/E2E-FinalDispute.test.ts), [`E2E-DisputeManager.test.ts`](../../../../../test/e2e/E2E-DisputeManager.test.ts); all-killed-window path: none — gap                                                                                                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-6.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `REQ-DIS-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-6.T1`.   | Partial; permutation audit pending | [`E2E-FinalDispute.test.ts`](../../../../../test/e2e/E2E-FinalDispute.test.ts), [`E2E-DisputeManager.test.ts`](../../../../../test/e2e/E2E-DisputeManager.test.ts); all-killed-window path: none — gap                                                                                                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-6.T1.P3`  | before/at/after deadline and maximum honest skew                                     | Verify `REQ-DIS-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-6.T1`.   | Partial; permutation audit pending | [`E2E-FinalDispute.test.ts`](../../../../../test/e2e/E2E-FinalDispute.test.ts), [`E2E-DisputeManager.test.ts`](../../../../../test/e2e/E2E-DisputeManager.test.ts); all-killed-window path: none — gap                                                                                                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-6.T1.P4`  | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-DIS-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-6.T1`.   | Partial; permutation audit pending | [`E2E-FinalDispute.test.ts`](../../../../../test/e2e/E2E-FinalDispute.test.ts), [`E2E-DisputeManager.test.ts`](../../../../../test/e2e/E2E-DisputeManager.test.ts); all-killed-window path: none — gap                                                                                                                                                                                                                                                                        | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-7.T1.P1`  | valid case and direct invalid/opposite                                               | Verify `INV-DIS-7` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-7.T1`.   | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`slashAndTimeout_ignoresTimeout`, `slashSuppressesTimeout`)                                                                                                                                                                                                                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-7.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `INV-DIS-7` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-7.T1`.   | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`slashAndTimeout_ignoresTimeout`, `slashSuppressesTimeout`)                                                                                                                                                                                                                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-7.T1.P3`  | before/at/after deadline and maximum honest skew                                     | Verify `INV-DIS-7` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-7.T1`.   | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`slashAndTimeout_ignoresTimeout`, `slashSuppressesTimeout`)                                                                                                                                                                                                                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-7.T1.P4`  | new/existing/removed/slashed participant and concurrent membership change            | Verify `INV-DIS-7` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-7.T1`.   | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`slashAndTimeout_ignoresTimeout`, `slashSuppressesTimeout`)                                                                                                                                                                                                                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-8.T1.P1`  | valid case and direct invalid/opposite                                               | Verify `INV-DIS-8` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-8.T1`.   | Partial; permutation audit pending | [`E2E-Timeouts.test.ts`](../../../../../test/e2e/E2E-Timeouts.test.ts); multi-missed-author ordering and empty-timeout divergence (§5): none — gap                                                                                                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-8.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `INV-DIS-8` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-8.T1`.   | Partial; permutation audit pending | [`E2E-Timeouts.test.ts`](../../../../../test/e2e/E2E-Timeouts.test.ts); multi-missed-author ordering and empty-timeout divergence (§5): none — gap                                                                                                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-8.T1.P3`  | correct/wrong/missing/duplicate/forged identity or signature and membership boundary | Verify `INV-DIS-8` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-8.T1`.   | Partial; permutation audit pending | [`E2E-Timeouts.test.ts`](../../../../../test/e2e/E2E-Timeouts.test.ts); multi-missed-author ordering and empty-timeout divergence (§5): none — gap                                                                                                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-DIS-8.T1.P4`  | before/at/after deadline and maximum honest skew                                     | Verify `INV-DIS-8` through the matching implementation conformance row and the concrete obligations refined from `INV-DIS-8.T1`.   | Partial; permutation audit pending | [`E2E-Timeouts.test.ts`](../../../../../test/e2e/E2E-Timeouts.test.ts); multi-missed-author ordering and empty-timeout divergence (§5): none — gap                                                                                                                                                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-9.T1.P1`  | valid case and direct invalid/opposite                                               | Verify `REQ-DIS-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-9.T1`.   | Partial; permutation audit pending | [`E2E-StateSnapshots.test.ts`](../../../../../test/e2e/E2E-StateSnapshots.test.ts), [`E2E-MaliciousUpdateSnapshot.test.ts`](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts); multi-generation adversarial walks: none — gap                                                                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-9.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `REQ-DIS-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-9.T1`.   | Partial; permutation audit pending | [`E2E-StateSnapshots.test.ts`](../../../../../test/e2e/E2E-StateSnapshots.test.ts), [`E2E-MaliciousUpdateSnapshot.test.ts`](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts); multi-generation adversarial walks: none — gap                                                                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-9.T1.P3`  | correct/wrong/missing/duplicate/forged identity or signature and membership boundary | Verify `REQ-DIS-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-9.T1`.   | Partial; permutation audit pending | [`E2E-StateSnapshots.test.ts`](../../../../../test/e2e/E2E-StateSnapshots.test.ts), [`E2E-MaliciousUpdateSnapshot.test.ts`](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts); multi-generation adversarial walks: none — gap                                                                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-9.T1.P4`  | before/at/after deadline and maximum honest skew                                     | Verify `REQ-DIS-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-9.T1`.   | Partial; permutation audit pending | [`E2E-StateSnapshots.test.ts`](../../../../../test/e2e/E2E-StateSnapshots.test.ts), [`E2E-MaliciousUpdateSnapshot.test.ts`](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts); multi-generation adversarial walks: none — gap                                                                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-9.T1.P5`  | duplicate/replay, every relevant permutation and concurrent delivery                 | Verify `REQ-DIS-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-9.T1`.   | Partial; permutation audit pending | [`E2E-StateSnapshots.test.ts`](../../../../../test/e2e/E2E-StateSnapshots.test.ts), [`E2E-MaliciousUpdateSnapshot.test.ts`](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts); multi-generation adversarial walks: none — gap                                                                                                                                                                                                                                      | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-10.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-DIS-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-10.T1`. | Partial; permutation audit pending | [`test/e2e/disputeValidation/disputeInputFields/timeout.test.ts`](../../../../../test/e2e/disputeValidation/disputeInputFields/timeout.test.ts), [`E2E-Timeouts.test.ts`](../../../../../test/e2e/E2E-Timeouts.test.ts), [`E2E-FirstBlockTimestampGrace.test.ts`](../../../../../test/e2e/E2E-FirstBlockTimestampGrace.test.ts), timeout-calldata tests in [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-10.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `REQ-DIS-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-10.T1`. | Partial; permutation audit pending | [`test/e2e/disputeValidation/disputeInputFields/timeout.test.ts`](../../../../../test/e2e/disputeValidation/disputeInputFields/timeout.test.ts), [`E2E-Timeouts.test.ts`](../../../../../test/e2e/E2E-Timeouts.test.ts), [`E2E-FirstBlockTimestampGrace.test.ts`](../../../../../test/e2e/E2E-FirstBlockTimestampGrace.test.ts), timeout-calldata tests in [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-10.T1.P3` | before/at/after deadline and maximum honest skew                                     | Verify `REQ-DIS-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-10.T1`. | Partial; permutation audit pending | [`test/e2e/disputeValidation/disputeInputFields/timeout.test.ts`](../../../../../test/e2e/disputeValidation/disputeInputFields/timeout.test.ts), [`E2E-Timeouts.test.ts`](../../../../../test/e2e/E2E-Timeouts.test.ts), [`E2E-FirstBlockTimestampGrace.test.ts`](../../../../../test/e2e/E2E-FirstBlockTimestampGrace.test.ts), timeout-calldata tests in [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-DIS-10.T1.P4` | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-DIS-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-DIS-10.T1`. | Partial; permutation audit pending | [`test/e2e/disputeValidation/disputeInputFields/timeout.test.ts`](../../../../../test/e2e/disputeValidation/disputeInputFields/timeout.test.ts), [`E2E-Timeouts.test.ts`](../../../../../test/e2e/E2E-Timeouts.test.ts), [`E2E-FirstBlockTimestampGrace.test.ts`](../../../../../test/e2e/E2E-FirstBlockTimestampGrace.test.ts), timeout-calldata tests in [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

## Implementation test traceability

This table judges every unit and internal system-integration permutation defined by the matching
implementation document. Evidence belongs here only after inspecting the exact test declaration.

| Implementation permutation                      | Level | Test status | Exact test evidence | Runtime coverage | Missing coverage                                      |
| ----------------------------------------------- | ----- | ----------- | ------------------- | ---------------- | ----------------------------------------------------- |
| _No numbered implementation permutations found_ | —     | Missing     | none — gap          | Not established  | Complete the matching implementation test plan first. |
