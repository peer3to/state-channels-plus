# Fraud Proofs & the On-Chain Slash Set — Verification

> **Agent authoring status:** Current verification evidence assembled; completeness and classifications require engineer verification.
> **Engineer verification:** Pending.

## Contents

- [Verification overview](#verification-overview)
    - [Specification-test adherence](#specification-test-adherence)
    - [Implementation-test adherence](#implementation-test-adherence)
    - [Contradictions](#contradictions)
    - [Missing](#missing)
- [Specification test traceability](#specification-test-traceability)
- [Implementation test traceability](#implementation-test-traceability)

## Verification overview

**Status:** Incomplete; candidate tests are known, but declaration-level setup and oracle review is
not complete for every specification and implementation permutation.

### Specification-test adherence

The existing candidate evidence is summarized below. These links are leads, not automatic credit; the traceability rows remain authoritative.

- **Facet unit/property tests:**
  `test/V1/StateChannelDiamondProxy/FraudProofFacet.t.sol`
  (timestamp-rule properties — an honest on-time block can never be slashed);
  `DisputeVerificationFacet.t.sol`
  (dispute-proof application windows, `DisputeBlockAuthorNotParticipant` matrix,
  `BlockInvalidStateTransition` wrong-turn case, timeout-calldata proof validation).
- **Pipeline e2e:**
  `E2E-FraudProofsBlockConfirmation.test.ts`
  (discovery through the confirmation pipeline),
  `E2E-ByzantineErrorAttribution.test.ts`
  (attribution of Byzantine failures), and the dispute fraud-proof soundness matrix in
  `test/e2e/disputeValidation/` (state-proof cases,
  input-field cases, not-latest-state, output-state, balance-invariant, inbound-hash).
- **Adversarial fuzzing:** `E2E-Fuzz-Dispute-MVP.test.ts`
  with `test/e2e/fuzz/disputeSoundnessMenu.ts`.
- **Gaps:** direct tests of the `applyFraudProofs` self-slash guard and of the skip-if-already-
  slashed idempotence: none — gap. Per-type positive/negative coverage for `BlockDoubleSign`,
  `WrongGenesis`, and `ForgedInboundMessageBlock` at the facet boundary: none — gap (they are
  exercised only indirectly through e2e dispute flows). Slash-set lifetime across parallel forks
  (§4 open question): none — gap.

Only rows with an exact declaration link and a specific setup/oracle assessment receive credit.
Rows that still cite a whole file, a neighboring behavior, or “none — gap” remain partial or missing.

### Implementation-test adherence

The matching implementation document defines the source-owned unit permutations and internal system
integration permutations. Each is listed in the bottom matrix. Until a real test declaration is inspected
and its setup, stimulus, oracle, failure behavior, and runtime are recorded, that permutation remains missing.

### Contradictions

No test has yet been confirmed to assert behavior opposite to this subject's specification or implementation
plan. This is not a clean bill of health: broad legacy links have been downgraded to partial or missing where
they do not prove the named permutation.

### Missing

- Replace every whole-file or descriptive evidence link with the exact declaration that performs the setup,
  stimulus, and assertion.
- Classify every applicable component, contract, integration, end-to-end, browser/node, worker, and
  distributed runtime boundary.
- Add or identify tests for every row marked missing; split tests whose oracle cannot prove each credited
  permutation independently.
- Recheck misleading or merely adjacent tests instead of using their filenames as evidence.

## Specification test traceability

This table judges every neutral specification permutation against the real test body. A file-level or adjacent test is not complete evidence.

| Permutation      | Behavior                                                                             | Implementation obligations                                                                                                     | Test status                        | Exact test evidence                                                                                                                                                                                                                                                                                                                                                                 | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ---------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `REQ-FP-1.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-FP-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-1.T1`. | Partial; permutation audit pending | [`E2E-FraudProofsBlockConfirmation.test.ts`](../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts), [`test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts`](../../../../../test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts)                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-1.T1.P2` | new/existing/removed/slashed participant and concurrent membership change            | Verify `REQ-FP-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-1.T1`. | Partial; permutation audit pending | [`E2E-FraudProofsBlockConfirmation.test.ts`](../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts), [`test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts`](../../../../../test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts)                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-1.T1.P3` | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-FP-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-1.T1`. | Partial; permutation audit pending | [`E2E-FraudProofsBlockConfirmation.test.ts`](../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts), [`test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts`](../../../../../test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts)                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-2.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-FP-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-2.T1`. | Partial; permutation audit pending | [`FraudProofFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/FraudProofFacet.t.sol) (`InvalidTimestamp` properties), [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`BlockInvalidStateTransition`); `BlockDoubleSign`/`WrongGenesis`/`ForgedInboundMessageBlock` facet-boundary tests: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-2.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `REQ-FP-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-2.T1`. | Partial; permutation audit pending | [`FraudProofFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/FraudProofFacet.t.sol) (`InvalidTimestamp` properties), [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`BlockInvalidStateTransition`); `BlockDoubleSign`/`WrongGenesis`/`ForgedInboundMessageBlock` facet-boundary tests: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-2.T1.P3` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary | Verify `REQ-FP-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-2.T1`. | Partial; permutation audit pending | [`FraudProofFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/FraudProofFacet.t.sol) (`InvalidTimestamp` properties), [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`BlockInvalidStateTransition`); `BlockDoubleSign`/`WrongGenesis`/`ForgedInboundMessageBlock` facet-boundary tests: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-2.T1.P4` | before/at/after deadline and maximum honest skew                                     | Verify `REQ-FP-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-2.T1`. | Partial; permutation audit pending | [`FraudProofFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/FraudProofFacet.t.sol) (`InvalidTimestamp` properties), [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`BlockInvalidStateTransition`); `BlockDoubleSign`/`WrongGenesis`/`ForgedInboundMessageBlock` facet-boundary tests: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-2.T1.P5` | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-FP-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-2.T1`. | Partial; permutation audit pending | [`FraudProofFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/FraudProofFacet.t.sol) (`InvalidTimestamp` properties), [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`BlockInvalidStateTransition`); `BlockDoubleSign`/`WrongGenesis`/`ForgedInboundMessageBlock` facet-boundary tests: none — gap | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-3.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-FP-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-3.T1`. | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`getOnChainSlashedParticipantsUpToTimestamp` prefix test)                                                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-3.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary | Verify `REQ-FP-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-3.T1`. | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`getOnChainSlashedParticipantsUpToTimestamp` prefix test)                                                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-3.T1.P3` | before/at/after deadline and maximum honest skew                                     | Verify `REQ-FP-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-3.T1`. | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`getOnChainSlashedParticipantsUpToTimestamp` prefix test)                                                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-3.T1.P4` | new/existing/removed/slashed participant and concurrent membership change            | Verify `REQ-FP-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-3.T1`. | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`getOnChainSlashedParticipantsUpToTimestamp` prefix test)                                                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-3.T1.P5` | duplicate/replay, every relevant permutation and concurrent delivery                 | Verify `REQ-FP-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-3.T1`. | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (`getOnChainSlashedParticipantsUpToTimestamp` prefix test)                                                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-4.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-FP-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-4.T1`. | Partial; permutation audit pending | [`E2E-StaleMembershipDispute.test.ts`](../../../../../test/e2e/E2E-StaleMembershipDispute.test.ts); direct disqualification unit test: none — gap                                                                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-4.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary | Verify `REQ-FP-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-4.T1`. | Partial; permutation audit pending | [`E2E-StaleMembershipDispute.test.ts`](../../../../../test/e2e/E2E-StaleMembershipDispute.test.ts); direct disqualification unit test: none — gap                                                                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-4.T1.P3` | new/existing/removed/slashed participant and concurrent membership change            | Verify `REQ-FP-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-4.T1`. | Partial; permutation audit pending | [`E2E-StaleMembershipDispute.test.ts`](../../../../../test/e2e/E2E-StaleMembershipDispute.test.ts); direct disqualification unit test: none — gap                                                                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-4.T1.P4` | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-FP-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-4.T1`. | Partial; permutation audit pending | [`E2E-StaleMembershipDispute.test.ts`](../../../../../test/e2e/E2E-StaleMembershipDispute.test.ts); direct disqualification unit test: none — gap                                                                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-5.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-FP-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-5.T1`. | Partial; permutation audit pending | [`test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts`](../../../../../test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts)                                                                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-5.T1.P2` | before/at/after deadline and maximum honest skew                                     | Verify `REQ-FP-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-5.T1`. | Partial; permutation audit pending | [`test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts`](../../../../../test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts)                                                                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-5.T1.P3` | new/existing/removed/slashed participant and concurrent membership change            | Verify `REQ-FP-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-5.T1`. | Partial; permutation audit pending | [`test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts`](../../../../../test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts)                                                                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-5.T1.P4` | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-FP-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-5.T1`. | Partial; permutation audit pending | [`test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts`](../../../../../test/e2e/disputeValidation/disputeInputFields/onChainSlashes.test.ts)                                                                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-6.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-FP-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-6.T1`. | Missing                            | none — gap                                                                                                                                                                                                                                                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-6.T1.P2` | new/existing/removed/slashed participant and concurrent membership change            | Verify `REQ-FP-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-6.T1`. | Missing                            | none — gap                                                                                                                                                                                                                                                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-6.T1.P3` | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-FP-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-6.T1`. | Missing                            | none — gap                                                                                                                                                                                                                                                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-7.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-FP-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-7.T1`. | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (expiry/atomic-batch tests), [`test/e2e/disputeValidation/`](../../../../../test/e2e/disputeValidation)                                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-7.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork          | Verify `REQ-FP-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-7.T1`. | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (expiry/atomic-batch tests), [`test/e2e/disputeValidation/`](../../../../../test/e2e/disputeValidation)                                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-7.T1.P3` | before/at/after deadline and maximum honest skew                                     | Verify `REQ-FP-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-7.T1`. | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (expiry/atomic-batch tests), [`test/e2e/disputeValidation/`](../../../../../test/e2e/disputeValidation)                                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-7.T1.P4` | new/existing/removed/slashed participant and concurrent membership change            | Verify `REQ-FP-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-7.T1`. | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (expiry/atomic-batch tests), [`test/e2e/disputeValidation/`](../../../../../test/e2e/disputeValidation)                                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-7.T1.P5` | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-FP-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-7.T1`. | Partial; permutation audit pending | [`DisputeVerificationFacet.t.sol`](../../../../../test/V1/StateChannelDiamondProxy/DisputeVerificationFacet.t.sol) (expiry/atomic-batch tests), [`test/e2e/disputeValidation/`](../../../../../test/e2e/disputeValidation)                                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-FP-8.T1.P1` | valid case and direct invalid/opposite                                               | Verify `INV-FP-8` through the matching implementation conformance row and the concrete obligations refined from `INV-FP-8.T1`. | Missing                            | none — gap                                                                                                                                                                                                                                                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-FP-8.T1.P2` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary | Verify `INV-FP-8` through the matching implementation conformance row and the concrete obligations refined from `INV-FP-8.T1`. | Missing                            | none — gap                                                                                                                                                                                                                                                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-FP-8.T1.P3` | new/existing/removed/slashed participant and concurrent membership change            | Verify `INV-FP-8` through the matching implementation conformance row and the concrete obligations refined from `INV-FP-8.T1`. | Missing                            | none — gap                                                                                                                                                                                                                                                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-FP-8.T1.P4` | duplicate/replay, every relevant permutation and concurrent delivery                 | Verify `INV-FP-8` through the matching implementation conformance row and the concrete obligations refined from `INV-FP-8.T1`. | Missing                            | none — gap                                                                                                                                                                                                                                                                                                                                                                          | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-9.T1.P1` | valid case and direct invalid/opposite                                               | Verify `REQ-FP-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-9.T1`. | Partial; permutation audit pending | tracked in [../security/open-security-review.md](../../audit/security-assessment.md)                                                                                                                                                                                                                                                                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-9.T1.P2` | malformed and adversarial input, partial failure, retry and recovery                 | Verify `REQ-FP-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-9.T1`. | Partial; permutation audit pending | tracked in [../security/open-security-review.md](../../audit/security-assessment.md)                                                                                                                                                                                                                                                                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-FP-9.T1.P3` | static review of every named alternative, omitted category and changed assumption    | Verify `REQ-FP-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-FP-9.T1`. | Partial; permutation audit pending | tracked in [../security/open-security-review.md](../../audit/security-assessment.md)                                                                                                                                                                                                                                                                                                | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

## Implementation test traceability

This table judges every unit and internal system-integration permutation defined by the matching
implementation document. Evidence belongs here only after inspecting the exact test declaration.

| Implementation permutation                      | Level | Test status | Exact test evidence | Runtime coverage | Missing coverage                                      |
| ----------------------------------------------- | ----- | ----------- | ------------------- | ---------------- | ----------------------------------------------------- |
| _No numbered implementation permutations found_ | —     | Missing     | none — gap          | Not established  | Complete the matching implementation test plan first. |
