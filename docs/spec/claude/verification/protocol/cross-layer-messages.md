# Cross-Layer Message Streams — Verification

> **Agent authoring status:** Current verification evidence assembled; completeness and classifications require engineer verification.
> **Engineer verification:** Pending.

## Contents

- [Verification overview](#verification-overview)
    - [Specification-test adherence](#specification-test-adherence)
    - [Implementation-test adherence](#implementation-test-adherence)
    - [Contradictions](#contradictions)
    - [Missing](#missing)
- [Specification test traceability](#specification-test-traceability)
- [Implementation test traceability](#implementation-test-traceability)

## Verification overview

**Status:** Incomplete; candidate tests are known, but declaration-level setup and oracle review is
not complete for every specification and implementation permutation.

### Specification-test adherence

The existing candidate evidence is summarized below. These links are leads, not automatic credit; the traceability rows remain authoritative.

Locally: range verification predicates (`_verifyOutboundMessageBlocks`,
`_verifyInboundMessageBlocks`, `_pruneOutboundMessageBlocks`) against crafted valid, truncated,
reordered, non-descendant, and duplicate ranges; totals mismatches; height gaps. Through
boundaries: snapshot advance with mixed processed/unprocessed ranges; consumer withdraw failure;
inbound forgery via block validation. System: full open → execute → exit lifecycles and
adversarial snapshot submissions. Existing evidence:
test/e2e/E2E-MaliciousUpdateSnapshot.test.ts,
test/e2e/E2E-StateSnapshots.test.ts,
test/stateManager/SnapshotUpdateService.test.ts.
Split-batch equivalence (REQ-MSG-5) and custom-message-type coverage: `none — gap`.

Same-fork advance with pending inbound (must be rejected), stale snapshots, forged milestone
proofs, tampered outbound ranges, withdrawal-cap violations, successor-fork adoption before and
after challenge expiry, and repeated/overlapping submissions. Evidence:
test/e2e/E2E-StateSnapshots.test.ts,
test/e2e/E2E-MaliciousUpdateSnapshot.test.ts,
test/stateManager/SnapshotUpdateService.test.ts,
test/e2e/E2E-ParticipantLifecycle.test.ts.

Honest sync, stale proofs, byzantine payloads at each abort point, DoS via repeated aborts, and
persistence of proved state. Evidence:
test/e2e/E2E-Spectate.test.ts,
test/e2e/E2E-SpectateStaleProofGuard.test.ts,
test/e2e/E2E-SpectatingAbortDoS.test.ts,
test/e2e/E2E-SpectatorStateProofPersistence.test.ts,
test/unit/SpectateService.test.ts.

Honest admission, race conditions on pinned snapshots, invalid/missing signatures, expired
deadlines, top-up of existing participants, deposit-without-inclusion plus force-join recovery,
and non-responsive peers during signature collection. Evidence:
test/e2e/E2E-JoinChannelRaceConditions.test.ts,
test/e2e/E2E-ForceJoinDispute.test.ts,
test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts,
test/V1/DiamondProxy/StateChannelManager/OpenChannel.test.ts,
test/e2e/E2E-ParticipantLifecycle.test.ts.

Verification: lifecycle exits and dispute-driven removals/slashes —
test/e2e/E2E-ParticipantLifecycle.test.ts,
test/e2e/E2E-Timeouts.test.ts,
test/e2e/E2E-DisputeManager.test.ts.

Honest late joins, colluding undercollateralized snapshots, malformed proofs, and mixed balance
models. Evidence:
test/e2e/disputeValidation/balanceInvariant.test.ts,
test/e2e/E2E-Spectate.test.ts (invariant runs inside
every successful sync), assertion helpers in
test/harness/actions/assert/AssertSnapshotActions.ts.
Mixed/composite asset-model coverage: `none — gap`.

Only rows with an exact declaration link and a specific setup/oracle assessment receive credit.
Rows that still cite a whole file, a neighboring behavior, or “none — gap” remain partial or missing.

### Implementation-test adherence

The matching implementation document defines the source-owned unit permutations and internal system
integration permutations. Each is listed in the bottom matrix. Until a real test declaration is inspected
and its setup, stimulus, oracle, failure behavior, and runtime are recorded, that permutation remains missing.

### Contradictions

No test has yet been confirmed to assert behavior opposite to this subject's specification or implementation
plan. This is not a clean bill of health: broad legacy links have been downgraded to partial or missing where
they do not prove the named permutation.

### Missing

- Replace every whole-file or descriptive evidence link with the exact declaration that performs the setup,
  stimulus, and assertion.
- Classify every applicable component, contract, integration, end-to-end, browser/node, worker, and
  distributed runtime boundary.
- Add or identify tests for every row marked missing; split tests whose oracle cannot prove each credited
  permutation independently.
- Recheck misleading or merely adjacent tests instead of using their filenames as evidence.

## Specification test traceability

This table judges every neutral specification permutation against the real test body. A file-level or adjacent test is not complete evidence.

| Permutation        | Behavior                                                                              | Implementation obligations                                                                                                         | Test status                        | Exact test evidence                                                                                                                                                                                                                                                | Runtime coverage                                                                                                                            | Missing coverage                                                        |
| ------------------ | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `INV-MSG-1.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-MSG-1` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-1.T1`.   | Partial; permutation audit pending | [E2E-MaliciousUpdateSnapshot.test.ts](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts)                                                                                                                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-MSG-1.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `INV-MSG-1` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-1.T1`.   | Partial; permutation audit pending | [E2E-MaliciousUpdateSnapshot.test.ts](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts)                                                                                                                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-MSG-1.T1.P3`  | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `INV-MSG-1` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-1.T1`.   | Partial; permutation audit pending | [E2E-MaliciousUpdateSnapshot.test.ts](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts)                                                                                                                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-MSG-2.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-MSG-2` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-2.T1`.   | Partial; permutation audit pending | [E2E-MaliciousUpdateSnapshot.test.ts](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts), [E2E-StateSnapshots.test.ts](../../../../../test/e2e/E2E-StateSnapshots.test.ts)                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-MSG-2.T1.P2`  | duplicate/replay, every relevant permutation and concurrent delivery                  | Verify `INV-MSG-2` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-2.T1`.   | Partial; permutation audit pending | [E2E-MaliciousUpdateSnapshot.test.ts](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts), [E2E-StateSnapshots.test.ts](../../../../../test/e2e/E2E-StateSnapshots.test.ts)                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-MSG-3.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-MSG-3` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-3.T1`.   | Partial; permutation audit pending | [E2E-StateSnapshots.test.ts](../../../../../test/e2e/E2E-StateSnapshots.test.ts)                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-MSG-3.T1.P2`  | zero, exact balance/boundary, one beyond, maximum value and conservation              | Verify `INV-MSG-3` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-3.T1`.   | Partial; permutation audit pending | [E2E-StateSnapshots.test.ts](../../../../../test/e2e/E2E-StateSnapshots.test.ts)                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-MSG-4.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-MSG-4` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-4.T1`.   | Partial; permutation audit pending | [E2E-MaliciousUpdateSnapshot.test.ts](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts)                                                                                                                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-MSG-4.T1.P2`  | zero, exact balance/boundary, one beyond, maximum value and conservation              | Verify `INV-MSG-4` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-4.T1`.   | Partial; permutation audit pending | [E2E-MaliciousUpdateSnapshot.test.ts](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts)                                                                                                                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-MSG-5.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-MSG-5` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-5.T1`.   | Partial; permutation audit pending | [SnapshotUpdateService.test.ts](../../../../../test/stateManager/SnapshotUpdateService.test.ts)                                                                                                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-MSG-5.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `INV-MSG-5` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-5.T1`.   | Partial; permutation audit pending | [SnapshotUpdateService.test.ts](../../../../../test/stateManager/SnapshotUpdateService.test.ts)                                                                                                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-MSG-6.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `INV-MSG-6` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-6.T1`.   | Partial; permutation audit pending | [balanceInvariant.test.ts](../../../../../test/e2e/disputeValidation/balanceInvariant.test.ts)                                                                                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `INV-MSG-6.T1.P2`  | zero, exact balance/boundary, one beyond, maximum value and conservation              | Verify `INV-MSG-6` through the matching implementation conformance row and the concrete obligations refined from `INV-MSG-6.T1`.   | Partial; permutation audit pending | [balanceInvariant.test.ts](../../../../../test/e2e/disputeValidation/balanceInvariant.test.ts)                                                                                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-1.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-MSG-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-1.T1`.   | Partial; permutation audit pending | [E2E-StateSnapshots.test.ts](../../../../../test/e2e/E2E-StateSnapshots.test.ts)                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-1.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-MSG-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-1.T1`.   | Partial; permutation audit pending | [E2E-StateSnapshots.test.ts](../../../../../test/e2e/E2E-StateSnapshots.test.ts)                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-1.T1.P3`  | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-MSG-1` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-1.T1`.   | Partial; permutation audit pending | [E2E-StateSnapshots.test.ts](../../../../../test/e2e/E2E-StateSnapshots.test.ts)                                                                                                                                                                                   | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-2.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-MSG-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-2.T1`.   | Partial; permutation audit pending | [test/e2e/disputeValidation](../../../../../test/e2e/disputeValidation)                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-2.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-MSG-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-2.T1`.   | Partial; permutation audit pending | [test/e2e/disputeValidation](../../../../../test/e2e/disputeValidation)                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-2.T1.P3`  | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-MSG-2` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-2.T1`.   | Partial; permutation audit pending | [test/e2e/disputeValidation](../../../../../test/e2e/disputeValidation)                                                                                                                                                                                            | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-3.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-MSG-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-3.T1`.   | Partial; permutation audit pending | [E2E-FraudProofsBlockConfirmation.test.ts](../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts)                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-3.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-MSG-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-3.T1`.   | Partial; permutation audit pending | [E2E-FraudProofsBlockConfirmation.test.ts](../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts)                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-3.T1.P3`  | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-MSG-3` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-3.T1`.   | Partial; permutation audit pending | [E2E-FraudProofsBlockConfirmation.test.ts](../../../../../test/e2e/E2E-FraudProofsBlockConfirmation.test.ts)                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-4.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-MSG-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-4.T1`.   | Partial; permutation audit pending | [E2E-MaliciousUpdateSnapshot.test.ts](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts)                                                                                                                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-4.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-MSG-4` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-4.T1`.   | Partial; permutation audit pending | [E2E-MaliciousUpdateSnapshot.test.ts](../../../../../test/e2e/E2E-MaliciousUpdateSnapshot.test.ts)                                                                                                                                                                 | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-5.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-MSG-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-5.T1`.   | Missing                            | none — gap (no split-batch equivalence test)                                                                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-5.T1.P2`  | zero/empty/no-op where meaningful, exact boundary, failure/recovery and relevant race | Verify `REQ-MSG-5` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-5.T1`.   | Missing                            | none — gap (no split-batch equivalence test)                                                                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-6.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-MSG-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-6.T1`.   | Partial; permutation audit pending | [E2E-StateSnapshots.test.ts](../../../../../test/e2e/E2E-StateSnapshots.test.ts), [E2E-FinalDispute.test.ts](../../../../../test/e2e/E2E-FinalDispute.test.ts)                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-6.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-MSG-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-6.T1`.   | Partial; permutation audit pending | [E2E-StateSnapshots.test.ts](../../../../../test/e2e/E2E-StateSnapshots.test.ts), [E2E-FinalDispute.test.ts](../../../../../test/e2e/E2E-FinalDispute.test.ts)                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-6.T1.P3`  | before/at/after deadline and maximum honest skew                                      | Verify `REQ-MSG-6` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-6.T1`.   | Partial; permutation audit pending | [E2E-StateSnapshots.test.ts](../../../../../test/e2e/E2E-StateSnapshots.test.ts), [E2E-FinalDispute.test.ts](../../../../../test/e2e/E2E-FinalDispute.test.ts)                                                                                                     | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-7.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-MSG-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-7.T1`.   | Partial; permutation audit pending | [SnapshotUpdateService.test.ts](../../../../../test/stateManager/SnapshotUpdateService.test.ts)                                                                                                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-7.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-MSG-7` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-7.T1`.   | Partial; permutation audit pending | [SnapshotUpdateService.test.ts](../../../../../test/stateManager/SnapshotUpdateService.test.ts)                                                                                                                                                                    | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-8.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-MSG-8` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-8.T1`.   | Partial; permutation audit pending | [E2E-ParticipantLifecycle.test.ts](../../../../../test/e2e/E2E-ParticipantLifecycle.test.ts)                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-8.T1.P2`  | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-MSG-8` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-8.T1`.   | Partial; permutation audit pending | [E2E-ParticipantLifecycle.test.ts](../../../../../test/e2e/E2E-ParticipantLifecycle.test.ts)                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-8.T1.P3`  | zero, exact balance/boundary, one beyond, maximum value and conservation              | Verify `REQ-MSG-8` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-8.T1`.   | Partial; permutation audit pending | [E2E-ParticipantLifecycle.test.ts](../../../../../test/e2e/E2E-ParticipantLifecycle.test.ts)                                                                                                                                                                       | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-9.T1.P1`  | valid case and direct invalid/opposite                                                | Verify `REQ-MSG-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-9.T1`.   | Partial; permutation audit pending | [E2E-Spectate.test.ts](../../../../../test/e2e/E2E-Spectate.test.ts), [E2E-SpectateStaleProofGuard.test.ts](../../../../../test/e2e/E2E-SpectateStaleProofGuard.test.ts), [E2E-SpectatingAbortDoS.test.ts](../../../../../test/e2e/E2E-SpectatingAbortDoS.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-9.T1.P2`  | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-MSG-9` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-9.T1`.   | Partial; permutation audit pending | [E2E-Spectate.test.ts](../../../../../test/e2e/E2E-Spectate.test.ts), [E2E-SpectateStaleProofGuard.test.ts](../../../../../test/e2e/E2E-SpectateStaleProofGuard.test.ts), [E2E-SpectatingAbortDoS.test.ts](../../../../../test/e2e/E2E-SpectatingAbortDoS.test.ts) | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-10.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-MSG-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-10.T1`. | Partial; permutation audit pending | [E2E-JoinChannelRaceConditions.test.ts](../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts), [JoinChannelSignatureRequest.test.ts](../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts)                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-10.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-MSG-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-10.T1`. | Partial; permutation audit pending | [E2E-JoinChannelRaceConditions.test.ts](../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts), [JoinChannelSignatureRequest.test.ts](../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts)                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-10.T1.P3` | correct/wrong/missing/duplicate/forged identity or signature and membership boundary  | Verify `REQ-MSG-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-10.T1`. | Partial; permutation audit pending | [E2E-JoinChannelRaceConditions.test.ts](../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts), [JoinChannelSignatureRequest.test.ts](../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts)                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-10.T1.P4` | new/existing/removed/slashed participant and concurrent membership change             | Verify `REQ-MSG-10` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-10.T1`. | Partial; permutation audit pending | [E2E-JoinChannelRaceConditions.test.ts](../../../../../test/e2e/E2E-JoinChannelRaceConditions.test.ts), [JoinChannelSignatureRequest.test.ts](../../../../../test/rpc/joinChannel/JoinChannelSignatureRequest.test.ts)                                             | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-11.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-MSG-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-11.T1`. | Partial; permutation audit pending | [E2E-ForceJoinDispute.test.ts](../../../../../test/e2e/E2E-ForceJoinDispute.test.ts)                                                                                                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-11.T1.P2` | zero, exact balance/boundary, one beyond, maximum value and conservation              | Verify `REQ-MSG-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-11.T1`. | Partial; permutation audit pending | [E2E-ForceJoinDispute.test.ts](../../../../../test/e2e/E2E-ForceJoinDispute.test.ts)                                                                                                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-11.T1.P3` | new/existing/removed/slashed participant and concurrent membership change             | Verify `REQ-MSG-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-11.T1`. | Partial; permutation audit pending | [E2E-ForceJoinDispute.test.ts](../../../../../test/e2e/E2E-ForceJoinDispute.test.ts)                                                                                                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-11.T1.P4` | malformed and adversarial input, partial failure, retry and recovery                  | Verify `REQ-MSG-11` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-11.T1`. | Partial; permutation audit pending | [E2E-ForceJoinDispute.test.ts](../../../../../test/e2e/E2E-ForceJoinDispute.test.ts)                                                                                                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-12.T1.P1` | valid case and direct invalid/opposite                                                | Verify `REQ-MSG-12` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-12.T1`. | Partial; permutation audit pending | [balanceInvariant.test.ts](../../../../../test/e2e/disputeValidation/balanceInvariant.test.ts), [E2E-Spectate.test.ts](../../../../../test/e2e/E2E-Spectate.test.ts)                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-12.T1.P2` | matching/mismatched commitment, predecessor/genesis, stale and foreign fork           | Verify `REQ-MSG-12` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-12.T1`. | Partial; permutation audit pending | [balanceInvariant.test.ts](../../../../../test/e2e/disputeValidation/balanceInvariant.test.ts), [E2E-Spectate.test.ts](../../../../../test/e2e/E2E-Spectate.test.ts)                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |
| `REQ-MSG-12.T1.P3` | zero, exact balance/boundary, one beyond, maximum value and conservation              | Verify `REQ-MSG-12` through the matching implementation conformance row and the concrete obligations refined from `REQ-MSG-12.T1`. | Partial; permutation audit pending | [balanceInvariant.test.ts](../../../../../test/e2e/disputeValidation/balanceInvariant.test.ts), [E2E-Spectate.test.ts](../../../../../test/e2e/E2E-Spectate.test.ts)                                                                                               | Not fully classified; enumerate every required component, integration, base-layer, worker, browser/node, and distributed mode that applies. | Map exact test declarations and account for every required permutation. |

## Implementation test traceability

This table judges every unit and internal system-integration permutation defined by the matching
implementation document. Evidence belongs here only after inspecting the exact test declaration.

| Implementation permutation                      | Level | Test status | Exact test evidence | Runtime coverage | Missing coverage                                      |
| ----------------------------------------------- | ----- | ----------- | ------------------- | ---------------- | ----------------------------------------------------- |
| _No numbered implementation permutations found_ | —     | Missing     | none — gap          | Not established  | Complete the matching implementation test plan first. |
